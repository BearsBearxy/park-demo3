# 2026-07-18 光伏分栋抄表实现计划

规范：docs/design/PV-METER-SPEC.md（用户已确认三决策：综合单价/日期记条汇月/13站按导图）。现有附表6（PvView）保持原样。

## Wave 1 后端（单代理）
- V36__pv_station.sql：建表+种子 13 站（一期 B座/C、D座/E座/F座/G座 sort 1-5；二期 8栋~13栋 sort 11-16；三期 创业大厦/工业大厦 sort 21-22）。
- V37__pv_reading.sql：建表+uk(station_id, read_date)。
- PvMeterController + PvMeterService：
  - GET /api/pv-meter/stations；POST/PUT/DELETE /api/pv-meter/stations（删除有记录 409）
  - GET /api/pv-meter/readings?year&month（可选 stationId）——返回行含 price_snap
  - POST/PUT/DELETE /api/pv-meter/readings（单条；POST/导入时 price_snap=当时站单价；PUT 不改 price_snap）
  - POST /api/pv-meter/import：行数组，(station,date) upsert，逐行结果（行级错误不整批拦），import_log 落后端（对齐既有导入端点风格——先读 ChargingController/PvController 现有 import 实现）
- BizException 中文错误、GET=viewer 可读、写=admin（SecurityConfig 无需改）。
- IT：种子 13 站锚点、reading CRUD+price_snap 快照语义（改站价后旧记录收益不变）、import 幂等重导、viewer 写 403。

## Wave 2 前端（两代理并行）
- **A 视图域**：views/pv/ 下新增 PvMeterView.vue（+抽屉/新增电站弹窗可内联）；改 PvView.vue 加功能门（两卡：月度汇总=原流程 / 分栋抄表=新屏；卡片样式同 SchedYearGate；会话内记住选择）。api/pvMeter.ts。布局遵 LIST-PAGE-SPEC + 工资条列宽铁律经验（定宽列+唯一弹性列）。行内编辑容量/单价用乐观更新（参考 bills paymap 教训：不 await 写库往返）。
- **B 导入导出域**：utils/importRegistry.ts 注册「光伏抄表」类型（表头别名/解析/校验/预览/入库 payload），utils/pvMeterExcel.ts（模板生成含示例行+月度导出），spec 测试。与 A 的接口=registry key 与 api/pvMeter.ts 契约（按 spec §3 写，不等 A）。

## Wave 3 校验（单代理）
- 前端 typecheck+全量 vitest；后端 mvnw 全量（基线：AnalysisApiIT 顺序污染连带 2 存量失败为已知）；grep 复核（功能门不破坏 PvView 原流程、registry 注册完整）。
- 【硬性】必须调用 StructuredOutput 返回。

## Wave 4 主会话浏览器验收
- 功能门双选可达且原附表6流程原样；13 站种子在位按期数分组；行内改单价→新录记录用新价、旧记录收益不变；抽屉增删改抄表条；导入模板下载→回导幂等；viewer 只读。

## 风险
- PvView 原屏结构未知（可能自带年份门）——功能门必须包在最外层,原流程零改动整体后移一步。
- price_snap 语义是本刀唯一微妙点,IT 必须锁住。
