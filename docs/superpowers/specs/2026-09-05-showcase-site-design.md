# demo3 产品展示站 · 设计稿 v1.2

- **状态**：设计稿，待用户审核。**未实施**，不含任何站点代码。
- **日期**：2026-09-05
- **原型**：`_design/showcase-site/prototype.html`（可滚动的动效原型；右下角「设计批注」开关逐章说明动效 / 转场 / 素材，并显示末尾的 UI 组件表）。Artifact：https://claude.ai/code/artifact/ee6e3b7f-c8c2-4f35-a2e8-7e640f6c191c
- **参照**：用户给的 https://www.apple.com/au/iphone-17-pro/ （章节式滚动叙事、钉住舞台、大字标题）。
- **上游**：`frontend/src/styles/tokens.css`、`frontend/src/assets/factory-park-mark.svg`、`frontend/src/nav/fpNav.ts`（屏名与 19 屏口径）、`docs/design/BILL-FORWARD-SPEC.md` §0.3（Excel 时代工作量盘点）、`docs/design/S20-BILL-DELIVERY-SPEC.md`（催缴单交付链）、`docs/design/POOL-ENGINE-SPEC.md`、`docs/design/CONCURRENCY-SPEC.md`、`docs/design/RBAC-SPEC.md`、`stage-review-2026-08-30.md`（工程数字）。

### 版本记录

| 版 | 变化 |
|---|---|
| v1 | 11 章 Apple 式滚动叙事；深夜蓝开场，进入报表章翻为纸白；Noto Sans SC 900 标题 |
| v1.1 | 用户要「Apple 简约克制 + UI 元素组件」：全系统字体 Semibold、Apple 色板、格不描边不投影、组件表 |
| **v1.2** | 用户三条指令：① 组件不用蓝，要高级的颜色 → **香槟表盘**（五案三镜头评审，D11）；② **全程黑底**，不再翻色（D2）；③ 卖点聚焦 **出账链自动出催缴单省时** 与 **自动分析 dashboard**，删「真实的 Excel 有多乱都行」导入章 → 叙事重排（§2.2、§4），三案三镜头评审后综合 |

---

## 0. 一句话

给 demo3 做一个**独立的产品展示站**：像 Apple 产品页那样一次滚动讲完两件事：**每月出账不再手填**（六步出账链算到每户催缴单），**数据录完分析就有了**（19 屏经营分析自动派生）。用真产品录屏做画面，用滚动驱动的转场把 11 个章节串成一条叙事，全程黑底。

### 设计判读（Design Read）

> 读作：面向**工业园区运营方与管理公司**（财务、运营主管、园区业主）的 B2B SaaS 产品展示页，语言是 **Apple 产品页式的章节滚动叙事**（全程黑底），视觉遵循 **Apple 的克制**：系统字体 + Semibold 承担层级，黑色单一页底，格不描边不投影，强调色（香槟金）只给按钮、链接、进行中状态、图表尾点；实现倾向 **原生 CSS + GSAP ScrollTrigger 钉住 / scrub**；标志沿用产品的三柱标志，站上为三档灰。

### 三档旋钮

| 旋钮 | 值 | 理由 |
|---|---|---|
| DESIGN_VARIANCE | 7 | Apple 式：大留白、单章单意、居中标题、舞台居中或 55/45 分栏 |
| MOTION_INTENSITY | 7 | 用户点名要 Scroll Animations；两个卖点章钉住 scrub 最重，其余轻。全部尊重 `prefers-reduced-motion` |
| VISUAL_DENSITY | 3 | 每章一个标题、一段 ≤ 40 字的导语、一个画面 |

---

## 1. 待拍板决策（D1–D13）

| # | 决策 | 我的取法 | 备选 / 状态 |
|---|---|---|---|
| D1 | 站点形态 | **独立静态站**，单页 + 锚点导航；与应用不同域名或子路径，不进 `frontend/` 的 Vue Router | 做成应用内路由（不建议：GSAP 与录屏拖进产品包体，size-check 会红） |
| D2 | 配色叙事 | **全程黑色**（用户 2026-09-05 拍板）。v1 / v1.1 的翻色撤回 | 已定 |
| D3 | 产品名与口号 | 原型暂用「园区管理系统」+ **「每月出账，不再手填。」**；页脚 © 行暂用同一产品名占位 | 需要正式对外产品名与版权主体（公司名还是产品名），两者一起换；口号候选见 §2.3 |
| D4 | 画面素材 | **真产品录屏 / 截图**（脱敏演示数据），不画插画、不做假界面。原型里全部是标注了尺寸与内容的「素材位」 | 用 AI 生成配图（不建议） |
| D5 | 转化动作 | 全站只有一个意图 **「预约演示」**（顶栏、首屏、尾屏同一文案）。无定价页、无自助注册 | 加「下载资料」第二意图 |
| D6 | 章节顺序 | 以前长这样 → 出账链 → **催缴单** → 转轴 → **经营分析** → 专题 → 公摊引擎 → 账簿报表 → 协作 → 大小园区 → 底座 → 预约演示（理由见 §2.2） | 评审第三案「dashboard 开场」：先看结果再讲来源；买家镜头判它削弱省时卖点 |
| D7 | 字体 | **全部系统字体**（SF Pro / PingFang SC / 微软雅黑），标题 Semibold 600，零字体下载 | 已定（v1.1） |
| D8 | 对外数字与名字 | 只用仓内坐实的数：152 / 209 / 920 / 24→13 / 51 / 17 / 19 屏 / 240+ / 2200+ / 100+。**6 家管理公司短名**出现在账簿章公司条上，对外发布前需你确认 | 公司条改「公司 A 到 F」占位 |
| D9 | 客户 logo 墙 | **不做**。没有可授权展示的真实客户 logo | 拿到授权后加在首屏之下 |
| D10 | 组件语言 | **Apple 网页组件对照实现**（§3.1）：局部导航、胶囊主按钮、尖角文字链、分段控件、格、勾选列表、脚注。首屏与章标题居中 | 沿用产品内 Factory Park DS 组件原样（后台工具感） |
| D11 | 强调色（不用蓝） | **香槟表盘**：按钮 `#C9B790` 墨字，悬停 `#D4C3A0`（只变亮），链接 `#D6C7A1`，异常 `#FF6E57`。五案三镜头评审合计 86 分居首。只平涂、只出现在四处，永不上标题、数字、发丝线、图标 | **雨过天青**（84 分）/ **铂金暖白**（81 分），见 §3.2 |
| D12 | 「大小园区」章去留 | **保留为轻章**（分段控件 tab，三问三答）。评审建议删（六问已被公摊 / 底座 / 协作 / 账簿四章回答），但你最初简报点名「解决大小客户的顾虑」 | 删掉，页面少一章更单 |
| D13 | 产品令牌是否同步 | **不同步**。产品截图与录屏里仍是蓝按钮与蓝三柱；对外口径：站的外壳不与产品抢色，画面里的蓝是「产品自己的颜色」；素材优先挑蓝色少的屏 | 给产品加一套香槟主题（产品改动，单独拍板） |

---

## 2. 受众与叙事弧

### 2.1 两类买家，一条叙事

| | 小客户（一个园区、一家管理公司、两三个人） | 大客户（多期园区、多家管理公司、几十个人） |
|---|---|---|
| 主要顾虑 | Excel 用得挺好为什么换；没有 IT；怕算错 | 同时录账互相覆盖；各公司账要分又要合；谁改了什么查不到 |
| 页面上的回答 | 以前长这样 + 催缴单（§4.2 / §4.4）、公摊引擎（§4.8）、底座（§4.12） | 协作（§4.10）、账簿报表（§4.9）、协作章的 RBAC 与审核 |
| 专门一章 | §4.11「大小园区」左面板 | 同章右面板 |

### 2.2 叙事顺序与重量

前三章只做一件事：让会计在第 1 章认出自己每月手填的那份 Excel（152 个 sheet / 209 个通知单块 / 920 条费项行），第 2 章看见六步怎么接成一条链，第 3 章在同一个 152 上看到「核对一遍再发」。卖点 a（省时出账）在全页前 40% 起落完成；横滚的终点直接放大成催缴单舞台，所以这一段是全页唯一一次连续劫持滚动。

第 4 章转轴用一句「没有一格是填进去的」和一根 1px 引线把读者从产物一（催缴单）牵到产物二（经营分析），同时隔开两个重章。第 5、6 章讲完卖点 b：先钉住看四个维度，再 Bento 平铺九个专题。

后五章（公摊全等 / 账簿 / 协作 / 大小园区 / 底座）全部不钉住，按买家顾虑的自然顺序排：怕算错、账要分又要合、多人会不会打架、我是哪种园区、能不能托付。公摊放在账簿前，因为它回答催缴单「算对没有」，账簿回答「对得上没有」，两者共同接住第 3 章留下的三方勾稽。尾屏回扣第 1 章的通知单，只留一个动作。

**重量分配**（屏高）：出账链 1.4 轻钉横滚 → 催缴单 2.6 钉住换画面（连续约 4.0 屏）；经营分析 2.6 钉住换画面；其余全部 sticky、经过式 scrub 或一次性。全站只在底座章计数一次数字。

### 2.3 口号候选（D3）

1. **每月出账，不再手填。**（原型采用；把会计的活写进第一行）
2. 六步一条链，算到催缴单。（已用作出账链章标题）
3. 每月，自己长出两样东西。（评审第二案首屏，强调两个产物并列）

### 2.4 文案铁律

- 不出现破折号「——」与「—」；不用「·」做万能分隔符（每行最多一个）。
- 不写「赋能 / 无缝 / 一站式 / 颠覆 / 极致 / 智慧 / 全方位」。动词要具体：算、派生、拦、对上、留痕。
- 数字只用仓内坐实的（D8），示例数据明确标「示例」。事实清单外的产品细节一律不写（如「按月锁定」「拓扑序」「下游全部重算」「审核员」「格式照旧」「每次提交都跑」「30 分钟」）。
- 每章一个标题（≤ 12 字）+ 一段导语（≤ 40 字）。产品内屏名照抄：**经营驾驶舱**（不是「驾驭舱」）。
- 眉标全站 3 枚：首屏「园区管理系统」、「催缴单」、「经营分析」。

---

## 3. 视觉系统（v1.2：黑底全程 + 香槟表盘）

原则一句话：**层级靠字重、字号、底色对比与留白，不靠颜色、描边、投影和字体切换。** 全站只有两种「亮」：白字与香槟金按钮。金只出现在按钮、链接、进行中的锁、图表尾点四处；异常色只出现在图表尾点（带 ≤ 12 字标注），图标一律次级灰。

| 角色 | 值 | 对比 / 规则 |
|---|---|---|
| 页面底 | `#000000` | 全程，不翻色（D2） |
| 格 / 纸 / 舞台 | `#1D1D1F` | 不描边、不投影 |
| 次级格（内嵌画面底、滑块、节点未点亮） | `#2C2C2E` | |
| 主文 / 次文 / 三级文 | `#F5F5F7` / `#A1A1A6` / `#86868B` | 三级文从 `#6E6E73` 提到 `#86868B`：12px 脚注对黑 5.8:1 过 AA（原 4.1:1 不过）；`#6E6E73` 只用于标志矮柱。**三级灰只落黑底与格色（对格 4.65:1），不落次级格（3.85:1 不过 AA）**：压次级格的小字（角标）用次级灰 |
| 发丝线 | `rgba(255,255,255,.14)` | 顶栏底边、问答分隔、出账链底线；**不许改成金色** |
| 控件边界 | `rgba(255,255,255,.36)` | 锁按钮描边（WCAG 1.4.11 非文字 ≥ 3:1） |
| 主按钮 | 底 `#C9B790`，字 `#1D1D1F` 17px Regular | 8.5:1（AAA）。HSL 41 / 35 / 68：饱和度压到 35，远看暖灰，近看才是金。亮度 l68 落在主文（l96）与次文（l64）之间，插进文字层级不打架。锁值允差：色相 ±3，饱和 ≤ 40 |
| 主按钮悬停 | `#D4C3A0` | 只变亮不变暗，与链接色排成一条明度梯 |
| 链接 | `#D6C7A1` | 对 `#000` 12.5:1，对格 10.1:1。独立文字链必带 ›，行内链接常驻下划线（对次文 `#A1A1A6` 只 1.5:1，链接不能只靠颜色被认出） |
| 进行中 | `= 主按钮`，不另设令牌 | 出账链当前节点实心；锁按钮「编辑中」1px 描边 + 文字；「请求接管」实底 |
| 图表 | 主线 `#A1A1A6`，正常带 8% 白，常态尾点 `#C9B790` r4，异常尾点 `#FF6E57` r5 + 标注 | 一张图只有一个彩色点；常态与异常靠形状加文字区分（两色亮度只差 1.4:1，红绿色弱下会混） |
| 异常语义色 | `#FF6E57` | 对格 6.1:1。只做图表异常尾点与 ≤ 12 字标注，每屏最多两处，不做底色，不上图标 |
| 标志 | 站上三柱改三档灰 `#6E6E73` / `#A1A1A6` / `#F5F5F7`（越高越亮） | 产品内 `factory-park-mark.svg` 不动。标志不上金，否则成第五处强调色，且与导航右侧的金色按钮抢眼 |
| 投影 | **无** | 全站零投影，叠放深度只靠 scale / opacity / y |

- **字阶**（全部系统字体，Semibold 600 承担层级）：首屏 `clamp(44px, 6vw, 80px)` / 1.06 / 字距 -0.012em；章标题 `clamp(32px, 4.1vw, 56px)` / 1.08 / -0.008em；眉标 21px / 600 / 次级灰；导语 21px / 1.42 / 400 / 次级灰；正文 17px / 1.47；说明 14px；脚注 12px。数字用同一字体 + `tabular-nums`，不用等宽字体。
- **圆角规则**（全站一致）：格 / 纸 / 舞台 28px；内嵌画面 18px；按钮、标签、分段控件 胶囊（980px）。
- **材质**：只有局部导航用半透明材质（页底色 78% + `blur(20px) saturate(180%)` + 1px 发丝线）；`prefers-reduced-transparency` 时回落为实底。批注卡是设计稿期专用。
- **图标**：沿用产品的 lucide（`gauge / siren / calendar-clock / scale-3d / bell-ring / sun / coins / layers / target / zap / plug / check / chevron-right`），24px，描边 1.5，次级灰，不上金、不上异常色。
- **动效克制**：滚动结构保留（用户要求），但零发光、零光环、零渐变纹理：链路线与揩痕都是 1px 白线，节点点亮只换底色。缓动统一 `power2.out / power3.out`，不用 expo 与回弹。

**验收硬规则（随配色）**：一屏之内只允许一块 ≥ 44px 的亮色实心胶囊（主按钮），标签限 32px、toast 用 14px 直角；金只许平涂，任何金属渐变、金色发丝线、金色大数字都是失败；实施期在一台会议室投影与一台普通办公显示器（含 Windows 夜间模式）上过稿，不只看 Mac（暖色温会把香槟推向黄铜）。

### 3.1 UI 组件表（Apple 网页组件对照）

原型末尾「附 UI 组件表」一节（只在批注开启时显示）把下面每一项都摆了出来。

| 组件 | Apple 对照 | 度量与规则 | 用在 |
|---|---|---|---|
| 局部导航 | localnav | 52px；材质 + 发丝线；标题 19px/600；链接 13px，当前章只变色不加下划线；右侧 28px 小胶囊按钮 | 全页常驻 |
| 主按钮 | `button` pill | 44px 高，胶囊，香槟 `#C9B790` 底墨字 17px Regular（Apple 主按钮不加粗）；悬停 `#D4C3A0` 只变亮；按下 `scale(.97)` 100ms；大号 52px / 19px | 首屏、尾屏、导航 |
| 文字链 | "Learn more ›" | `#D6C7A1` 17px + 18px 尖角箭头，悬停下划线；**次级动作一律用它，不做幽灵按钮** | 首屏 |
| 眉标 + 标题 + 导语 | typography-eyebrow / headline / intro | 21/600 次级灰 → 56/600 → 21/400 次级灰；居中；导语 ≤ 34em | 每章头部 |
| 分段控件 | segmented control | 轨道 = 格色，滑块 = 次级格色，3px 内距，胶囊。**两种职责**：经营分析章由滚动进度驱动（四段吸附，可点，点某段滚到对应进度）；大小园区章是普通 tab（滑块 380ms `cubic-bezier(.32,.72,0,1)`，面板 300ms 交叉淡入）；`role=tablist` | 经营分析、大小园区 |
| 格 | tile | 28px 圆角，24px 内距，无描边无投影；内嵌画面 18px | 舞台、Bento、双窗、底座 |
| 叠纸 | 叠放的 tile | 同格；sticky 叠放时前一张 `scale .94 / opacity .45 / y -24` | 以前长这样 |
| 标签 / 公司条 | 选中态反转 | 32px 胶囊，格色；选中 = 白底黑字，不用金 | 公司条 |
| 角标 | 小标签 | 22px 胶囊，次级格底，11px 600 次级灰（对次级格 5.4:1）：「示例」「示例数据」「账簿与报表」 | 舞台右上、Bento |
| 勾选列表 | checkmark list | `check` 图标 18px / 2.2 描边 + 15px 次级灰文字，横排 | 协作三条规则 |
| 锁按钮三态 | 与产品 `FPEditModeButton` 同形 | 150px 定宽胶囊，描边 `.36` 承担控件边界；换文案不换宽度；「编辑中」1px 香槟描边，「请求接管」香槟底 | 协作双窗 |
| 引线 | 出账链链路线换方向 | 1px 白色竖线，`scaleY 0→1` 随滚动 scrub | 转轴章 |
| 素材位 | 画面容器 | 格色底 + 左下 12px 三级灰说明；无虚线框、无渐变 | 全部录屏 / 截图位 |
| 脚注 | footnote | 12px / 1.5 三级灰；「示例」与工程数字口径在页脚统一说明 | Bento、页脚 |

**不用的 Apple 元素**（YAGNI）：图片上的「+」展开按钮、全屏模态、主题手动开关、渐变文字。

### 3.2 强调色三案（D11 备选，三镜头评审合计分）

| 案 | 按钮 / 字 | 链接 | 异常 | 合计 | 一句话 |
|---|---|---|---|---|---|
| **香槟表盘**（推荐） | `#C9B790` / `#1D1D1F`（8.5:1） | `#D6C7A1` | `#FF6E57` | 86 | 银行贵宾卡与表盘的低饱和金，黑底上不发光；风险是滑向「售楼处黑金」，靠「只平涂、只四处」守住 |
| 雨过天青 | `#8EC4B7` / `#1D1D1F`（8.6:1） | `#A0DCCE` | `#DB9338` | 84 | 宋瓷天青，最东方、最不像同行；风险是廉价屏与护眼模式把 0.06 彩度吞成灰，且财务语境「绿 = 跌」 |
| 铂金暖白 | `#EFE9DF` / `#1D1D1F`（13.9:1） | `#D8D0C4` | `#FF453A` | 81 | 黑金卡语法，颜色几乎缺席；风险是链接与正文只差 1.4:1，全靠 › 与下划线被认出 |

落选：墨翠（77，按钮 `#127749` 对黑只 3.8:1 太沉，且绿在 A 股语境读作亏）、古铜（73，平涂深铜在普通显示器上读作棕）。

---

## 4. 页面结构与逐章动效

滚动距离用「屏高」计（1 屏高 = 100vh）。「钉住」= GSAP `ScrollTrigger pin: true, start: "top top"`，`scrub` 值为跟手平滑秒数。所有动效只动 `transform / opacity / clip-path`。

### 4.0 局部导航（常驻）

52px 高，半透明材质，左三档灰标志 + 产品名，中间 6 个章节锚点（出账链 / 催缴单 / 经营分析 / 公摊引擎 / 账簿报表 / 协作；当前章变色，`ScrollTrigger` 按章节中线切换），右「预约演示」28px 小胶囊。桌面端单行，< 900px 隐藏中间锚点。

### 4.1 首屏「每月出账，不再手填。」

| | |
|---|---|
| 版式 | 居中：眉标「园区管理系统」→ 一行 80px 标题 → 导语「合同、抄表、公摊算完，每户催缴单自动派生。以前每月手填的表，核对一遍就发。」→ 主按钮「预约演示」+ 文字链「看它怎么出账 ›」（锚到出账链）。舞台在按钮之下（16:8）；视口较矮时只露出上半部 |
| 载入 | 文案四项依次上浮（`y 22→0`，错峰 0.08s，power3.out 0.9s）；舞台 `y 60→0` 升入 |
| 滚动 | 文案区 `y 0→-60, opacity 1→0`，scrub 跟随首屏滚出的前 55%；舞台留位，被第 1 章第一张 Excel 纸在同一位置接走 |
| 素材 | 录屏 A：**催缴单屏**「指定收款公司、核对确认、导出」，1440×900，10s 循环。不再用首页链路条（仓内 `billingChain.ts` 只有 5 步，与「六步」不一致） |

### 4.2 以前长这样「一个月的账，以前长这样。」

| | |
|---|---|
| 版式 | 居中标题 + 导语「两期园区 2024 年 3 月的真实盘点。每一格，以前都是会计每月手填的。」；三行大数字 **152 个租户 sheet / 209 个通知单块 / 920 条费项行**（Semibold，`tabular-nums`，静态不计数）；说明一行「24 种费项名，归一后 13 类。48 户一张 sheet 上叠 2 到 5 张通知单，版式 7 型。」；下方三张「纸」sticky 叠放，纸宽 880px |
| 动效 | 纸张 sticky 叠放，不钉住：每张 `position: sticky; top: 0`，下一张从视口底走到顶时上一张 `scale 1→.94, opacity 1→.45, y 0→-24`（scrub）。叠放动效与「48 户一张 sheet 叠 2 到 5 张」是同一件事 |
| 素材 | 截图 ×3：真实 Excel 通知单 sheet（2024-03，脱敏租户名 / 金额 / 房号）：单位置户、厂房加宿舍共用单户、一张 sheet 叠四张通知单的户。白底原样不调色 |
| 事实来源 | `BILL-FORWARD-SPEC.md` §0.3 |

### 4.3 出账链「六步一条链，算到催缴单。」

| | |
|---|---|
| 版式 | 章标题 + 导语「每月一次工序，六步。前一步的输出是后一步的输入，最后一步是每户一张催缴单。」先正常滚过；随后 100vh 横滚区轻钉，六张步骤卡（440px 宽，16:10 截图 + 步名 + 一行说明）横向排开，卡顶 1px 白线串六个节点；末卡「催缴单」同为 16:10，是下一章入口 |
| 钉住 | 1.4 屏高等效（横向平移距离 = 轨道宽 − 视口宽，`invalidateOnRefresh`），scrub 0.8 |
| 变化 | 轨道 `x 0→-距离` 线性；节点按 `i/6` 点亮：未到 `#2C2C2E`，**当前 `#C9B790`**（进行中状态），已过 `#F5F5F7`；节点间线段 `scaleX 0→1` 与轨道同速 |
| 转场出 | 末卡到位即解钉，接催缴单章：末卡与下一章第一张画面是同一张催缴单屏，内容衔接 |
| 移动端 | 不钉、不劫持：原生横向 `scroll-snap`，链路线全亮 |
| 六步文案 | 合同管理「合同计费行，是每户应收的起点。」/ 计费参数「口径做成参数位，怪癖照收，不顺手修正。」/ 园区抄表「尖峰平谷四段读数，反向电量当场拦截告警。」/ 公共电核算「51 个公摊池，与原册逐格验算全等。」/ 楼栋损耗「17 行楼栋损耗，逐栋落账。」/ 催缴单「前五步的结果，在这里变成每户一张通知单。」 |
| 素材 | 截图 ×6，1120×700，同一账期同一公司 |

### 4.4 催缴单「算出来，发出去。」（全页最重）

| | |
|---|---|
| 版式 | 55/45 分栏：左 16:10 舞台钉住，右侧眉标「催缴单」+ 标题 + 四段说明同位叠放，一次只显一段 |
| 入口 | 横滚解钉后正常滚动进章，舞台入视口时 `scale .82→1` 一次（origin 左，power3.out 0.6s）。真正的几何连续（末卡 FLIP 成本章舞台）是新功能，本稿不做 |
| 钉住 | 2.6 屏高，scrub 0.5 |
| 四段 | ①「按户一行，按月派生。」来源是合同计费行、抄表读数、计费参数 ②「租金表加水电表，就是那张通知单。」现实里发给租户的那份，系统按户生成 ③「指定收款公司，核对，导出就发。」导出 Excel 通知单与对账表，会计的活从「填」变成「核」 ④「三方勾稽。」催缴单应收、附表10 应收、台账实收对得上。**以前 152 个 sheet 逐格手填，现在核对一遍再发**（回扣第 1 章） |
| 变化 | 每段：上一段说明 `y→-32, opacity→0`；上一张画面 `opacity→0, scale→.985`；下一段 `y 32→0` 上浮；下一张 `opacity 0→1, scale 1.03→1`；段间停 0.6 单位，末段停 0.8 后解钉 |
| 素材 | 截图 ×4，1440×900：本月催缴单列表（按户一行）/ 单户通知单预览（租金表加水电表）/ 导出后的 Excel 通知单与对账表（Excel 打开真实导出文件，脱敏）/ 报表层「收入核对」屏（三方勾稽的落点，角标「账簿与报表」）。前三张角标「示例数据」 |
| 事实来源 | `S20-BILL-DELIVERY-SPEC.md`、`BILL-FORWARD-SPEC.md` §0.1、`2026-07-02-demo3-p2e-reconciliation-design.md` |

### 4.5 转轴「没有一格是填进去的。」

| | |
|---|---|
| 版式 | 纯排版过桥，约 0.6 屏高，无素材：居中标题 + 导语「录入归你，派生归系统。催缴单算完，经营分析同时就有了。」；一枚「结论条」小格（格色：「经营驾驶舱」+「本月应收、能耗、光伏、异常，都从这个月的录入数据派生。」+ 角标「示例」）；其下 1px 白色竖线画到本章底边，正接经营分析章顶边（章头在线下，舞台再下） |
| 动效 | 引线：竖线 `scaleY 0→1` 随章节穿过视口 scrub；标题与小格一次性上浮。它就是出账链那根链路线换了方向 |
| 作用 | 隔开两个重章，完成 a 到 b 的掉头 |

### 4.6 经营分析「数据录完，分析就有了。」（第二重）

| | |
|---|---|
| 版式 | 全宽居中：眉标「经营分析」+ 标题 + 导语「19 屏，含异常提醒中心，全由录入数据派生。每屏一条结论、一张图，点回原表。」；下方四段分段控件「驾驶舱 / 园区 / 租户 / 管理公司」；再下 16:9 舞台（最大 1120px；上一章引线接到本章顶边）；舞台下一行 17px 说明随画面切换；脚注「数据不足时显示引导，不放假数字。」 |
| 钉住 | 2.6 屏高，scrub 0.5 |
| 变化 | 四张画面交叉淡入（同催缴单章）；分段控件滑块 `xPercent` 由时间线驱动，在四段吸附；`aria-selected` 随进度更新；控件可点，点某段 `scrollTo` 对应进度。与催缴单章的区分：那章 55/45 分栏说明换段，这章全宽舞台，控件当进度条 |
| 四段说明 | 一眼看全园：出租、应收、能耗、光伏、异常放在一屏 / 园区维度：出租与楼栋，园区能耗 / 租户维度：用能与缴费，结构与续约 / 管理公司维度：利润表、资产负债、现金流、费用与报销 |
| 素材 | 录屏 B：经营驾驶舱，含一次维度切换，1440×900，8s 循环；截图 ×3：出租与楼栋 / 用能与缴费 / 利润表分析 |
| 事实来源 | `fpNav.ts` 经营分析层（驾驶舱 1 + 园区 2 + 租户 2 + 管理公司 4 + 专题 9 + 异常提醒中心 1 = 19） |

### 4.7 专题「一个专题，答一个问题。」

| | |
|---|---|
| 版式 | 居中标题 + 导语「九个专题加一个异常提醒中心。一个专题只答一个经营问题，都能点回原表。」；12 格 Bento，4 列 × 3 行恰好铺满：异常提醒中心 2×1（真截图裁 2:1，条目示例）+ 租户流失预警 + 到期墙与续约 / 光伏分栋分析 2×1（真实 SVG 示例图，尾点 `#FF6E57` + 标注「跌出正常带 9 天」，角标「示例」）+ 盈亏平衡与敏感性 + 损益附表分析 / 预算对比 + 光伏投资回收 + 电费成本分析 + 充电桩分析。1×1 格 = lucide 图标 + 名称，全部格色，无描边无投影 |
| 变化 | 进入视口错峰上浮（全站一次性上浮共用 `y 24→0`，错峰 0.07s，power2.out 0.8s）；光伏折线在格进入视口 80% 时 `stroke-dashoffset` 画一次 1.2s。光伏 SVG 的 viewBox 跟随渲染尺寸（用户单位 = CSS px），11px 标注不被压缩 |
| 素材 | 截图 ×1：异常提醒中心，裁 2:1；SVG ×1：站内手绘示例图 |
| 事实来源 | `fpNav.ts` 专题分析组 9 屏 + 监控组；`PV-ANALYSIS-SPEC.md` v2.1 L1 看板（这栋 ÷ 全园同刻度中位 + 正常范围带） |

### 4.8 公摊引擎「你的 Excel，逐格全等。」

| | |
|---|---|
| 版式 | 45/55 分栏：左标题 + 导语「51 个公摊池加 17 行损耗，与原册逐格验算全等。口径怪癖做成参数位，不顺手修正。」+ 两个大数字（公摊池 51 / 楼栋损耗行 17，静态）；右 16:10 舞台叠两层：底层 Excel「公共电分摊明细」2024-02 截图（左上标签「原 Excel 工作表」），顶层系统「公共电核算」截图（右上标签「系统」）。「差异 0」由标题「全等」说出，不做第三个数字 |
| 动效 | 经过式 scrub，不钉住：舞台从视口 80% 走到 20% 的滚动里，顶层 `clip-path inset(0 100% 0 0) → inset(0)` 从左向右揩开，1px 白线同步移动，75% 处左上标签淡出 |
| 素材 | 截图 ×2，行列构图对齐，否则揩开时没有「同一张表」的错觉 |
| 事实来源 | `POOL-ENGINE-SPEC.md`、`POOL-FORMULA-AUDIT-2024-02.md` |

### 4.9 账簿与报表「一套账，多家公司。」

| | |
|---|---|
| 版式 | 居中标题 + 导语「6 家管理公司各一本册，三大报表加 5 张损益附表。全部汇总，就是跨公司求和。」；公司条「全部汇总」选中态反转 + 6 枚 32px 公司标签（对外名见 D8）；一块 16:9 舞台放利润表截图（全部汇总口径）；说明一行「收入核对：附表10 应收对台账实收。催缴单章三方勾稽的另两方，在这里。」 |
| 动效 | 一次性上浮，不钉住，一屏一图（叠放家族已给第 1 章）。无投影 |
| 素材 | 截图 ×1：利润表（全部汇总口径） |

### 4.10 协作「多人同时做账，不打架。」

| | |
|---|---|
| 版式 | 居中标题 + 导语「读全开，写分权。要改先占锁，谁在编辑一眼可见；急事可授权接管，全程留审计。」；两扇并排窗口（陈会计 / 王会计 各自的月度台账屏），顶栏在场头像，标题行右侧 150px 定宽三态锁按钮；底部勾选列表三条「读全开 / 写按 11 个业务模块分权 / 一张表 × 一个月审核，审核后只读」 |
| 动效 | 不钉住。进入视口播放一次约 3.3s：左窗按钮按下 `scale .96` 回弹，「编辑模式」→「完成」并上 1px 香槟描边，两侧头像浮现；0.5s 后右窗「编辑模式」→「陈会计 正在编辑」；0.6s 后 →「请求接管」（香槟底）；0.5s 后右窗底部浮出「已向在线主管发送接管请求，授权后锁归你，双方留名审计（示例）」。播完停在终态；减少动态效果时直接显示终态 |
| 素材 | 截图 ×2：月度台账 编辑态 / 浏览态（两个账号），1120×700 |
| 事实来源 | `CONCURRENCY-SPEC.md`、`RBAC-SPEC.md` v2、侧栏重设计 spec §7（审核粒度） |

### 4.11 大小园区「两个人，或几十个人的园区。」（D12）

居中标题下一枚分段控件 tab：「一个园区，一家管理公司」/「多期园区，多家公司」，默认小园区。面板各三条问答（问句真引号，答句 ≤ 40 字，只用事实清单内容），左问右答两栏，条间一条发丝线。切换：滑块 380ms，面板 300ms 交叉淡入；`role=tablist / tab / tabpanel`，键盘可达。无素材。

| 面板 | 问 | 答 |
|---|---|---|
| 小园区 | “Excel 用得挺好，为什么要换？” | 每月手填的 920 条费项行，以后由合同、读数、参数算出来。不再手抄，不换习惯。 |
| 小园区 | “我们没有 IT 部门。” | Docker Compose 私有化部署，Caddy 自动 HTTPS。数据在你自己的服务器上。 |
| 小园区 | “怕系统算错，还不如自己算。” | 51 个公摊池、17 行损耗与原册逐格验算全等；口径做成参数位，你能看见、能改。 |
| 大园区 | “几个人同时录账，会不会互相覆盖？” | 编辑锁加在场头像，谁在改一眼看见；急事可授权接管，改动全部留审计。 |
| 大园区 | “各公司的账要分开，又要合并看。” | 6 家管理公司各一本独立册，全部汇总就是跨公司求和，收入核对把应收与实收对上。 |
| 大园区 | “谁改了什么，要查得到。” | 写按 11 个业务模块分权，操作日志可追溯；审核过的表只读。 |

### 4.12 底座「看不见的地方，也认真做。」

导语「私有化部署，数据留在你自己的服务器。下面这些数字，是它被验证的方式。」四格：`240+` 条真库集成测试 / `2200+` 条前端断言 / `100+` 次数据库迁移 / 一格文字「Docker Compose 私有化部署，Caddy 自动 HTTPS。JWT 登录防爆破，操作全程审计。」数字进入视口 1.4s 计数一次（**全站唯一一处计数**）。上线前重核数字。

### 4.13 预约演示

居中标题「带上通知单，来看一场演示。」+ 导语「用示例园区，当场走一遍从抄表到催缴单，再到经营分析。」+ 大号「预约演示」。不写时长，不承诺用客户自己的 Excel 当场派生（见 §10）。页脚一行：标志 + 产品名 + 版权；脚注统一说明「示例」与数字口径。

---

## 5. 相邻章节转场总表

| 从 → 到 | 手法 | 为什么这样接 |
|---|---|---|
| 首屏 → 以前长这样 | 首屏文案上移淡出，舞台留位；第一张 Excel 纸在同一水平位置升入 | 「这块屏以前长这样」：产品画面与它替代的 Excel 在同一位置交接 |
| 以前长这样 → 出账链 | 第三张纸滚出，出账链标题正常出现，横滚区再钉 | 两个重手法之间留一段正常滚动 |
| 出账链 → 催缴单 | 横滚末卡「催缴单」到位即解钉；解钉后正常滚动进章，催缴单舞台入视口时 `scale .82→1` 一次 | 末卡与下一章第一张画面是同一张催缴单屏，内容衔接；全页唯一一次连续劫持 |
| 催缴单 → 转轴 | 解钉后一段正常滚动进转轴章 | 卸掉重量，准备掉头 |
| 转轴 → 经营分析 | 1px 竖线从结论条小格底部画到经营分析章顶边（章头在线下） | 「产物一」牵到「产物二」；线是出账链链路线换了方向 |
| 经营分析 → 专题 | 解钉接 Bento 平铺 | 钉住换画面（竖向）接平铺（横向），版式家族切换 |
| 专题 → 公摊引擎 → 账簿 → 协作 → 大小园区 → 底座 → 预约 | 全部正常滚动 + 一次性上浮 / 经过式 scrub | 后六章是说服与收口，不再劫持滚动，让人能快速读完并点按钮 |

版式家族顺序（不相邻重复）：纸张 sticky 叠放 → 横向平移 → 钉住换画面（55/45 说明换段）→ 引线纯排版 → 钉住换画面（全宽 + 分段控件当进度）→ Bento → clip-path 揩开（不钉）→ 一次性上浮 + 公司条 → 双窗三态（一次性）→ 分段控件问答 → 四格数字计数 → 居中收口。钉住换画面出现两次但不相邻、底盘不同，是两个卖点章共享同一重量级的刻意安排。

---

## 6. 素材清单

全部来自真产品，演示库需先脱敏（租户名、账号、金额、房号可换成演示数据；6 家公司名按 D8 决定）。

| 编号 | 类型 | 内容 | 规格 | 用在 |
|---|---|---|---|---|
| A | 录屏 | 催缴单屏：指定收款公司、核对确认、导出 | 1440×900，10s 循环，WebM(VP9) + MP4(H.264) + 首帧 WebP | 首屏 |
| B | 录屏 | 经营驾驶舱，含一次维度切换 | 1440×900，8s 循环 | 经营分析 |
| 1–3 | 截图 | 真实 Excel 通知单 sheet：单位置户 / 厂房加宿舍共用单户 / 叠四张通知单的户（2024-03） | 按 sheet 原始比例裁到 1120 宽，2× 导出，脱敏 | 以前长这样 |
| 4–9 | 截图 | 合同管理 / 计费参数 / 园区抄表 / 公共电核算 / 楼栋损耗 / 催缴单 | 1120×700，同一账期同一公司 | 出账链 |
| 10–13 | 截图 | 本月催缴单列表 / 单户通知单预览 / Excel 打开的真实导出通知单与对账表 / 收入核对屏 | 1440×900 | 催缴单 |
| 14–16 | 截图 | 出租与楼栋 / 用能与缴费 / 利润表分析 | 1440×900 | 经营分析 |
| 17 | 截图 | 异常提醒中心 | 1440×900，裁 2:1，条目示例 | 专题 |
| 18–19 | 截图 | Excel「公共电分摊明细」2024-02 / 公共电核算屏（同期） | 行列对齐 | 公摊引擎 |
| 20 | 截图 | 利润表（全部汇总口径） | 1440×900 | 账簿报表 |
| 21–22 | 截图 | 月度台账 编辑态 / 浏览态（两个账号） | 1120×700 | 协作 |

录制约定：浏览器 1440×900 无书签栏、浅色系统主题、演示库、光标隐藏；截图导出 2× 供高分屏；优先挑蓝色按钮少的画面（D13）。

---

## 7. 技术方案

| 项 | 取法 | 理由 |
|---|---|---|
| 构建 | Vite 静态站（纯 HTML + CSS + 少量 JS，或 Vue 3 单文件组件），独立目录 `site/`，独立部署（Caddy 多加一个 host 块） | 与团队现有工具链一致；不进产品包体 |
| 动效 | **GSAP 3 + ScrollTrigger**（钉住 / scrub / 横移 / sticky 叠放的退后）；一次性上浮用 `ScrollTrigger.batch` 或 CSS `animation-timeline: view()` | 两个卖点章要 pin + scrub，这是 GSAP 的主场；不与其他动效库混用 |
| 禁止 | `window.addEventListener('scroll')`、在框架状态里存滚动进度、动 `top/left/width/height` | 逐帧重排；只动 transform / opacity / clip-path |
| 录屏 | `<video muted autoplay loop playsinline>`，WebM 优先 MP4 兜底，`poster` 首帧 WebP；进入视口才 `play()`，离开 `pause()` | 首屏 LCP 靠 poster |
| 配色 | 单一黑色主题；全站颜色只走 CSS 变量 | 换强调色只改一处 |
| 减少动态效果 | `gsap.matchMedia()` 三档：桌面 / 移动 / `prefers-reduced-motion`。reduce 档：不钉住、不横移，所有终态直接可读 | 非可选项 |
| 移动端（< 900px） | 全部不钉住；出账链改原生横向 scroll-snap；Bento 两列；双窗上下排；分段控件回到普通 tab | 小屏劫持滚动体验差 |
| 字体 | **零字体下载**：系统栈 | Apple 自己就用系统字体；LCP 不等字体 |
| 性能预算 | LCP < 2.5s（poster 预加载）、CLS < 0.1（所有舞台定死 aspect-ratio）、首包 JS < 60KB | |
| 可访问性 | 顶栏与按钮键盘可达、可见焦点环（香槟 2px）；录屏有 `aria-label`；分段控件 `role=tablist`；批注卡与组件表只在设计稿期存在 | |
| SEO | 单页，每章 `<section id>`，标题层级 h1 → h2 → h3；OG 图用首屏截图 | |

---

## 8. 不做什么

- 不做定价页、不做自助注册、不做客户 logo 墙（D5 / D9）。
- 不做插画、不做 3D、不做粒子；画面全部是真产品。
- 不做双语；不做暗 / 亮手动开关（全程黑）。
- 不在产品仓 `frontend/` 里加路由或依赖；不为展示站改产品令牌（D13）。
- 不写事实清单外的数字与产品细节。

---

## 9. 验收标准（实施期）

1. 桌面 1440 宽：三个钉住章节（出账链 / 催缴单 / 经营分析）的 `start` 都是 `top top`，钉住期间无跳动（`anticipatePin: 1`），解钉后无空白。
2. 全程黑底：任何滚动位置 `body` 背景都是 #000，无翻色。
3. 系统开「减少动态效果」：页面无任何钉住与位移，全部文字与终态可读。
4. 390 宽手机：无横向溢出；出账链可横向拨动并吸附；所有素材位 aspect-ratio 定死，CLS < 0.1。
5. 视觉字符串扫描：全站 0 个「—」「——」；每行「·」≤ 1；眉标 ≤ 3 枚；0 个「驾驭舱」（产品屏名是「驾驶舱」）。
6. 「预约演示」三处文案完全一致；无第二个同意图按钮。
7. 强调色审计：香槟只出现在主按钮、文字链、出账链当前节点、锁按钮进行中、图表常态尾点；标题、数字、发丝线、图标、公司条上 0 处金；全站 0 处渐变；一屏之内只有一块 ≥ 44px 的亮色实心胶囊。
8. 链接审计：每个独立文字链带 ›；每个行内链接有下划线。
9. 异常色审计：只出现在图表尾点与 ≤ 12 字标注；每处都与文字或图标同现；0 处底色。
10. 双设备过稿：一台会议室投影 + 一台普通办公显示器（开 Windows 夜间模式）各看一遍，香槟未漂成黄铜。
11. Lighthouse 桌面：Performance ≥ 90，Accessibility ≥ 95。

---

## 10. 待确认清单（数字与口径，评审提出）

1. **省时口径**：Excel 时代每月出账要几个人、几天？系统上线后一个月催缴单从派生到导出实际多久？这是主线最值钱的一个数；拿到前首屏与第 1 章只写「不再手填」。
2. 「152 户催缴单一次派生」能否对外说：2024-03 盘点的两期 152 户是否已全部建成合同计费行并成功派生过一个月？没有的话催缴单章第四段只写「以前 152 个 sheet 逐格手填」。
3. 「209 个通知单块」对外能否写成「209 张通知单」？本稿全部用「块」。
4. 「19 屏」口径：`fpNav.ts` 经营分析层 = 18 屏 + 异常提醒中心 = 19。本稿写「19 屏，含异常提醒中心」。
5. 出账链步数：首页链路条 5 步（无合同管理），本稿按六步写；首页录屏已弃用。
6. 6 家管理公司短名可否对外出现在账簿章公司条（D8）。
7. 尾屏是否可承诺「带上你们的 Excel，当场算出这个月的催缴单」？导入器目前只吃过这一家的 7 型版式，本稿改为「用示例园区当场走一遍」；演示时长不写。
8. 第 1 章三张真实 Excel 通知单 sheet 的具体选取与脱敏范围。
9. 底座三个数字上线按保守下限（240+ / 2200+ / 100+）还是当天实际值。
10. 催缴单章第四画面用报表层「收入核对」屏代表三方勾稽：请确认该屏同时对得上附表10 应收与台账实收。
11. 异常提醒中心 Bento 格条目：示例假条目，还是真阈值口径（收缴率 / 能耗环比 / 收入中断 / 负值行 / 欠费）。
12. 转轴章「点回原表」：深链粒度是整屏还是逐条结论。

---

## 11. 与原型的对应

原型 `_design/showcase-site/prototype.html` 已实现 §4 全部动效与转场（GSAP 3.12.5 via cdnjs），用「素材位」占住 §6 全部位置并标注规格；右下角「设计批注」开关显示 / 隐藏逐章说明与末尾的「UI 组件表」（§3.1 的实物）。它是设计稿，不是站点代码：实施时按 §7 重建，不直接复用。**文案以本 spec §4 为准，原型只是其实物**；两者不一致时改原型。

实施要点（v1.2 对抗复查坐实，写进代码即可）：钉住的 ScrollTrigger 必须先于其余触发器创建（否则后者按无钉住的位置算，会提前几千像素触发）；减少动态效果与无脚本两档的协作双窗直接呈现播放终态；经营分析分段控件在静态档要能当普通 tab 切换四张画面；光伏 SVG 的 viewBox 跟随渲染尺寸。
