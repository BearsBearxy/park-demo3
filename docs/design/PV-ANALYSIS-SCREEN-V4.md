# 光伏分栋分析屏 v4 版式与可视化规范（PV-ANALYSIS-SCREEN-V4）

拍板：**2026-09-13**，用户看过 v2 画布第一页四块板（Main / Abs / Ledger / Drawer，含已并入的 A2 与 B8-a）后定稿。
画布：artifact `https://claude.ai/code/artifact/8e9131e4-e060-45dc-a92d-90f84f1f2097`；源 `../运维文档/设计稿/已实现/光伏分栋分析v4定稿-2026-09-13/*.dc.html`（`Main` / `Abs` / `Ledger` / `Drawer` 四个文件；`Options*` / `Queue*` 是对照板，不是规格）。
**画布是规格，本文是它的索引与口径说明；两者不一致以画布为准。**

上游：`PV-ANALYSIS-SPEC.md`（数据口径、公式、判据线、护栏仍以它为准，逐条对照见 §0）。

---

## §0 相对 v3（PV-ANALYSIS-SPEC §06）作废了什么、保留了什么

「作废」= v3 那条写法不再成立（含被改写的）；「保留」= 照原条执行，v4 画板只是换了承载它的图。

| v3 条目（§号） | v4 | 为什么 |
|---|---|---|
| §6.0 四层按物理属性排他（L0 sunken 无卡头 / L1 白底卡头 / L2 带坐标轴 / L3 覆盖层） | **作废** → 全屏统一分析层卡片 `.av2-card`；层级靠位置（KPI 行 → 主卡 → 段控 → 档内卡片），不靠「有无坐标轴 / 有无卡头」 | 用户：「完全没有对齐我产品的视觉设计规范，整个页面摆在一起即杂乱又毫无设计感」；其余 18 个分析屏都是 av2 卡片栅格 |
| §6.0 / §6.2 全屏唯一 26px 主数 | **作废** → `AnaKpiTile` 数值 16px mono 600 | 同上；分析层 KPI 瓦现网是 `--fs-h3` |
| §6.0 / §6.2 B0 = `--surface-sunken` 实底 + 3px 左竖条的五格指标带，高 76 | **作废** → 6 个 `AnaKpiTile` 白底瓦（`.av2-kpis`） | 同上 |
| §6.0 / §6.7 强调色 `#9D5D17` 只在 L0 / L1 | **作废** → 警示琥珀 `#EF9F27`（字色 `#854F0B`）全屏可用 | 用户：「所有图的颜色全都是黑白灰，根本什么都看不出来」 |
| §6.7 全屏色相限 4 个（墨 + 蓝家族 + 橙 + 黄），类别色只许 B7 / B8 / L6 | **作废** → 色板取 `anaTheme.ts` 的 `CAT_COLORS`（蓝 / 青 / 琥珀 / 红）+ 浅蓝族，期别三色上岗 | 同上 |
| §6.7 不用红 | **作废** → `#E24B4A` 给低于下沿、变点、观测值、超出外带 | 同上；红只标方向与越界，不做严重度灯（§01「不做红黄灯」仍成立） |
| §6.7 13 栋没有任何一栋拥有自己的颜色 | **作废** → 按期别三色着色（一期 `#378ADD` / 二期 `#5DCAA5` / 三期 `#EF9F27`）；单栋仍不拥有私有色 | 同上 |
| §6.7 群体（其余栋）走 `--ink-300`；焦点蓝 `#3675B2` | **作废** → 群体走浅蓝带 `#B5D4F4`；焦点 / 主序列 `#378ADD` | 同上 |
| §6.5b / §6.7 L2 自相关、L4 零分布「故意留墨」 | **作废** → L2 蓝柱 + 淡带，L4 浅蓝直方 + 红观测线 | 同上 |
| §6.5b 强调色不进高级分析档，全走墨阶 | **作废** → 高级分析档与其余档同一套色板 | 同上 |
| §6.7 缺抄用暖黄 `--hue-yellow`，「黄 ≠ 橙」 | **作废** → 缺抄与出范围同用琥珀 `#EF9F27`，靠形状区分（缺抄 = 底部刻度记号 / 格底色；出范围 = r4 白环点 / 段底色） | 本轮任务书定的系列色：琥珀 = 警示（出范围、缺抄） |
| §6.7 B7 自消纳 `--fill-slate` / 上网 `--fill-cyan` / 损耗 `--ink-300` | **作废** → 自用 `#378ADD` / 上网 `#85B7EB` / 损耗 墨 30% | 用户：「所有图的颜色全都是黑白灰」 |
| §6.7 质量矩阵 正常 `--fill-sky` / 缺抄 `--hue-yellow` / 整日剔除 `--fill-slate` / 未投产留白（格级四态） | **作废** → 日格四态：全齐 蓝 16% / 缺抄 琥珀 30% / 整日剔除 墨 10% + 45° 划痕 / 还没到 虚线空格；**未投产不再是格态**，挪到缺抄榜行尾灰字「未投产」 | 日历按天折叠后格子没有「栋」这一维，「未投产」不是某一天的状态 |
| §6.7 字号 20 / 26 / 14 / 12 / 11，字重只用 400 / 600 | **作废** → 16 / 14 / 12 / 11（本屏不用 20），字重 400 / 500 / 600 | 同 §6.2 那行；500 来自芯片与段控的 tokens |
| §6.1 首屏预算 457 ≤ 463；命中 > 4 栋时队列内滚、主卡钉高 317 | **作废** → 首屏实测 548，超预算 86（§2）；无队列、无内滚，主卡钉高 386 | 队列形态被否（§5）；超预算怎么办见 §4 |
| §6.3 B1 左右两栏（268px 队列 + 1fr 大图） | **作废** → 13 枚楼栋芯片一行 + 通栏大图 | 用户：「逐月比值的楼栋列表又长又挤」；列表第二次被否：「更加无法接受…全部挤在一起的列表」 |
| §6.3 队列四组（出范围段 / 未低于你设的线 / 读不出 / 尚未投产），组可折叠不可消失 | **作废** → 芯片三态：命中（方向色 10% 底 + 同色字 + 天数徽标）/ 未命中（白底）/ 未投产（灰字禁用） | 同上；「读不出」上不上芯片见 §4 |
| §6.3 出范围点 r3.4 琥珀，其余点 r1.9 墨 45% | **作废** → 在范围内不画点；低于下沿 `#E24B4A`、高于上沿 `#EF9F27`，r4 白环 | 用户：「所有图的颜色全都是黑白灰」 |
| §6.3 连续段一律加琥珀底色 | **作废** → 段底色随方向（低于 红 10% / 高于 琥珀 10%），仍夹进绘图区 | 同上 |
| §3.8 未到画「虚线竖界 + 『未到（M/D 起）』字 + x 轴刻度淡化」；漏抄画「底部缺口记号 + 『漏』字」 | **作废（文字标注部分）** → 未到只画淡底 `rgba(28,28,28,.03)`，漏抄只画折线断开 + 底部 2×7 琥珀刻度，不写「未到 / 漏」字 | 按画板；两态仍是两种视觉（见下方「保留」） |
| §6.5 B3 用 ECharts，选中栋深线 | **作废** → 自绘 SVG，三条线尾直标、无图例 | 09-12 起分析层需要直标 + 深底气泡的图一律自绘（`AnaForecastChart.vue` 头注） |
| §6.5 B4 Tufte slopegraph（年档替 B3） | **作废** → 并入 A2 最右「比去年」一列 | 协调方看截图实测：11 条线挤在 190 h 窄带里，左端六个标签叠成一团、中段线交叉成结 |
| §6.5 B5 Cleveland 点图 + 从锚点长出的细杆 | **作废** → A2「比 807.5 h 多出多少」横条 | 协调方实测：横轴从 780 起、锚点贴最左，11 根杆几乎一样长，又成了一堵墙；三期橙与锚点橙撞色 |
| §6.5 B4、B5 两张卡 | **作废** → 一张 A2（s12） | B4 与 B5 画的是同一张 13 行的表（一个比去年、一个比标杆），两张卡说一件事 |
| §6.5 B6 ECharts 散点 + `markArea` ±3%，4 × 250 | **作废** → 自绘；s12 卡，左 300 正方图、右说明栏 | ±3% 是沿对角线的斜带，`markArea` 只能画轴对齐矩形；B4/B5 并卡后 B6 独占半行 |
| §6.5 B8 ECharts 分组横向柱 | **作废** → 堆叠横条 + 条尾合计 | 分组条读合计要心算；用户在对照板上选 B8-a |
| §6.5b L1 α 用 ECharts 点 + 块自助 95% 区间 | **作废** → 横向排名条（期别色）+ 同色 30% 延长带 | 用户：「先天水平 α 排序现在的可视化选择很丑」 |
| §6.5b L3 内联 SVG 小倍数 13 行 × 12 月 | **作废** → 13 × 12 热力格 | 用户：「各栋残差的年内走势可视化的选择所有楼栋摆在一起，宽度也窄全部都是一条线」 |
| §6.5b L5a 抛光收敛轨迹 + L5b 换个扫描顺序名次对照，两张卡 | **作废** → α 卡脚注一句大白话（迷你线 2026-09-13 也删了） | 用户：「抛光收敛轨迹的质感跟我在 RStudio 上产出的用于论文的矢量图一样」「『换个扫描顺序，名次动不动』这个我更是没看懂想表达什么意思」「抛光可信吗没看懂什么意思」 |
| §6.5b L5「行优先 / 列优先两条轨迹都画」 | **作废** → 不画轨迹 | 同上；那张卡已不存在 |
| §6.5b L7 完整检验表 11 列（z / p / q / N_eff / 变点区间 / σ 怎么估） | **作废** → 7 列，表脚一句说明砍了哪些 | 屏上不写统计名词 |
| §05 p / q / σ / 置信区间「只在高级分析档出现」+ 双向断言 | **作废** → 任何档都不写 | 同上；段控说明句与 α 卡脚注两处遗留见 §4 |
| §6.6 抽屉入口两个：主卡卡头 + B4 任意线段 | **作废** → 只剩主卡卡头「看 X 的整年 →」 | B4 已并入 A2；A2 行要不要当入口见 §4 |
| §6.6 B9 估计范围「渐变透明、不描硬边」 | **作废** → `#B5D4F4` 50% 实填、不描边 | 按画板 |
| §6.6 B11 逐月 β + ±1 标准误淡带，高 170 | **作废** → 12 个月槽点线（r7 白环），无带，高 200 | 按画板；空槽直接写原因，不再靠满格灰带表示「不确定」 |
| §6.7 96px 栋名列在「队列 / bullet / slopegraph / 抽屉表」四处同宽 | **作废** → 那四个载体里三个已不存在；现存栋名列见 §3 各块 | 对象没了；A2 栋名列是否对齐 96 见 §4 |
| §6.7 窄屏「队列从大图左侧改到上方」 | **作废** → 无队列 | 同 §6.3 那行 |
| §02 数据模型（V118 `metered` / V121 板数两列 / V122–V123 参数） | **保留**，本轮不动 | 版式改动不涉及数据结构 |
| §3.1 零剔除：表离线 = NaN 绝不补齐；`gen = 0` 有太阳却发 0 与离线分开 | **保留** | 数据口径 |
| §3.3 变点必须给区间（`ciLo / ciHi`），不能只给一个点 | **保留**（B9 画板未画区间阴影，见 §4） | 数据口径 |
| §3.5 年粒度锚点今天就能用；月粒度基准第二年起；第一年不画月度基准线 | **保留** | 数据口径 |
| §3.7 估带窗口三条件：同批在网 ∩ 同运行状态 ∩ 变点之前 | **保留** | 数据口径 |
| §3.7 估带窗口必须写在屏上（`baseNote`） | **保留**（画板主卡未画，放哪见 §4） | 它决定了什么算「出范围」 |
| §3.8 刻度三态：已抄 / 漏抄 / 未到必须是两种视觉两种算法，绝不合并成同一种灰 | **保留**（B1：漏抄折线断开 + 底部琥珀刻度；未到淡底，不画点不连线不补 0） | 数据口径 |
| §3.8 / §00 v3 #3 覆盖率分母 = 已过去的刻度数，不是整段 | **保留**（KPI「已过去 28/31 天」） | 月中打开不能全屏「读不出」 |
| §3.8 x 轴永远画满整段，不随录到哪天伸缩 | **保留**（B1 / B3 / B7 x 轴都画到 31 日） | 明天再打开不整体位移 |
| §3.8 范围带照常画满整宽 | **保留**（B1 带画满 1–31） | 带来自段外窗口 |
| §3.8 段末端正好是数据截止日时是「仍在持续」，不写闭区间 | **保留**（`Run.live`；画板未覆盖） | 闭区间暗示已结束 |
| §3.8 跨过漏抄的一天不算断；段 = 连续 ≥ 3 个**已抄**刻度同向 | **保留** | 记录的缺口不是现象的缺口 |
| §4.2 判据线定值（2 倍稳健波动 / 连续 3 刻度 / 散点门槛 = 噪声期望 3 倍 / 覆盖 ≥ 90% / 台账差 ±3% / 年等效 ≥ 锚点 × 85%），全部读参数中心 | **保留**（画板判据脚写「≥ 60%」是错的示意值，见 §3.3） | 判据线是人定的，必须可改 |
| §4.3 #1 只列命中的；底部固定「未列出 ≠ 没问题」 | **保留**（画板没有这句，见 §4） | 屏做不到「正常」这个保证 |
| §4.3 #3 第三档「读不出」；措辞用「未低于你设的线」不用「正常」 | **保留**（画板芯片没有这一态、也没有这句措辞，见 §4） | 阈值是用户设的 |
| §05 屏只说明可视化在做什么，不输出解释性结论 | **保留**（画板有 4 处与之冲突，见 §4） | 事实能写，模型得出的结论不写 |
| §6.2 主数统计段所在的栋数，不数点（段 = 连续 ≥ 3 刻度同向） | **保留**（KPI「本段出范围栋数」） | |
| §6.2 新鲜度不能删：没有它「0 栋触线」与「数据没更新」长得一样 | **保留**（「数据到」瓦 + 「已过去 N/M 天」；「已抄 n/N · 覆盖 x% · 算于」画板缺，见 §4） | |
| §6.2 0 命中时高度不塌 | **保留**（`AnaKpiTile` 恒高；0 命中时副行转 muted） | 版面不跳 |
| §6.3 一次只画一栋；每张图用它自己的量程，不共用纵轴、不钳位 | **保留**（B1 纵轴按选中栋自适应） | 共用纵轴会吃掉最该被看见的那一栋 |
| §6.3 点任意一栋都能换图，包括未命中的 | **保留**（13 枚芯片都可点，未投产两枚除外） | 判据线决定谁进命中组，不决定谁能被看 |
| §6.3 排序键印在行上（按出范围刻度数降序） | **保留**（芯片：有连续段在前 → 出范围天数降序，徽标 = 排序键） | 顺序可复算、可反对 |
| §6.3 图头一行：栋名 + 三段式事实句 | **保留**（大图图头） | |
| §6.4 判据脚：参数当前值原文 + `去改`，线与越线的画面同卡 | **保留** | 「越线了」才退回成可复算的事实 |
| §6.4 年档「连续 ≥ 3 刻度算一段」灰掉划掉 + 「（年档不出此判据）」 | **作废**（2026-09-13 用户拍板） | 划掉的同时 KPI 出范围栋数、芯片、事实句仍按连续段算，屏上自相矛盾；改为年档也按「连续 ≥ 3 个月」算段，判据脚照常显示 |
| §6.5 切档不换卡，容器高度按「绝对水平 / 账面量」较高者钉死；高级分析档不抬 min-height | **保留**（v4 钉 1200，§2） | 切换时页面不跳 |
| §6.5 默认停在「账面量」 | **保留**（画板停在「高级分析」只为展示） | 板数录进来之前只有这一档是全真数 |
| §6.5 B3 不画 13 条线，画分布（各栋 p25~p75 带 + 全园中位 + 选中栋） | **保留** | 叠加折线可用上限约 8 条 |
| §6.5 B3 在网不足 3 栋的刻度留空；全园线漏抄日按当天在网栋算 | **保留** | |
| §6.5 B6 必须正方且两轴同 min / max | **保留** | 非等比画布上 y=x 不是 45° |
| §6.5 B7 副轴固定 0–6%；超出的刻度折线断开 + 轴外三角 + 数值 | **保留** | 副轴按物理区间取 |
| §6.5 B8 是累计量：月中标题改「截至 M/D 累计」，提示「不可与整月直接比」 | **保留**（画板只画了整月标题） | 最容易被当成整月的一张 |
| §6.5 B4 / B5（→ A2）年口径；在网不足 90 天移出并图注计数；不做年化 | **保留** | 年化 20 天的样本是撒谎 |
| §6.5 非零基线必须在图注里交代 | **保留**（A2 图注「横轴不从 0 h 起」） | 否则是悄悄截断 |
| §6.5b 「高级分析」懒算：没点开时 `buildLab` 一次都不跑 | **保留** | |
| §6.5b 高级分析各块卡头说出自己吃的期间（整年 / 显示段） | **保留**（画板只有 L3 当段列头、L6 图例「2025 年 8 月」说了，其余块实施时补） | 工作台同时有两个期间 |
| §6.5b L2 滞后上限 `min(30, ⌊n/4⌋)`，不许写死 30 | **保留** | n=31 时 lag 30 只有一对样本 |
| §6.5b 观测窗口不写死「最后 30 天」；月档取当段，退回了要说出来 | **保留** | |
| §6.5b L4 与 L7 的数不同源 → L4 把自己的尾概率印在观测线上 | **保留**（「比它更极端的只有 N/1000 次」） | |
| §6.5b L6 CSS Grid 手写（bundle 无 `HeatmapChart` / `VisualMapComponent`） | **保留** | 用了是空白图，jsdom 测不出 |
| §6.5b L6 月档按自然月铺满，不按 `dates` 铺 | **保留**（画板画满 1–31） | 格位稳定 |
| §6.5b 整日剔除 ≠ 全园一栋没抄，两者要分得开 | **保留**（剔除格 45° 划痕 + 「整日剔除」字） | 两个不同的审计答案 |
| §6.5b 不带回反事实金额、逐栋判词字段、`TestRow.status` 结论列 | **保留** | §01 明令禁止 |
| §6.6 点队列行（→ 芯片）= 换图，不开抽屉 | **保留** | 抽屉会盖住刚换的图 |
| §6.6 抽屉「上一栋 / 下一栋」按当前排序走 | **保留**（按芯片顺序） | |
| §6.6 关抽屉时期间档位、页面滚动位置、段控档位一律不变 | **保留** | 验收项 |
| §6.6 抽屉三张图固定按年；当段底色只画到数据截止日 | **保留**（B9 当前期间底色画到 28 日） | |
| §6.6 B9 `rcsTrend` 点数 < 节点数 + 2 → 整块不画 | **保留** | |
| §6.6 B10 控制限用变点前段估；前段 < 8 点退回全期并如实标出估计窗口 | **保留**（B10 底条 + 直标估计窗口） | |
| §6.6 B11 样本不足整格不画并写「样本不足」 | **保留**（空槽刻度下写「不足」） | |
| §6.6 B12 未到的行不出现；漏抄的行出现、值为空 + 标记 | **保留**（「没抄表」行） | |
| §6.6 抽屉整体：在网 < 8 天不出图，写原因 | **保留** | |
| §6.6 runs rule：数据不够时只能不报，不能报「未违规」 | **保留** | 没触发和没数据是两件事 |
| §6.7 参照线 = `--ink-500` + 虚线（锚点 / y=x / β=1 / 零线） | **保留**（A2 锚点、B6 对角线、B11 =1 线；锚点线从画板早期的橙改回墨阶，§1） | |
| §6.7「损耗不是一种蓝」：自用与上网是有用输出，损耗是废掉的 | **保留**（损耗走墨阶） | |
| §6.7 方向 / 状态不能只靠颜色：▲▼、位置、点半径、字重 | **保留**（A2 ▲▼；B10 点半径三档） | |
| §6.7 轴字 11px，任何情况不下调到 10 | **保留**（自绘图轴字同为 11px） | |
| §6.7 间距：栅格 gap 8 / 卡内边距 10–12 / 圆角 8 | **保留** | |
| §6.7 窄屏断点：≤1280 s8→12、s4→6；≤1100 全 12；≤600 `.hint-desk` 隐藏；验收 1366×768 与 2560 | **保留**（「队列改到上方」那句作废，见上） | |
| §07 `metered = 0` 未装表：分析屏整站不出现 | **保留** | 与漏抄是两回事 |
| §07 未录板数或单块标称 W：B6 不画点、图注「n 栋未录」；等效小时分母退回 `capacity_kwp` 并写明口径 | **保留** | |
| §07 当日在网 < 3：该日不出 β，原始值照常进账面图 | **保留** | |
| §07 在网 < 90 天：「读不出」，不出判据结论 | **保留** | 显「未低于线」就是假绿 |
| §07 某月覆盖 < 90%：记一行，该格变浅 | **保留** | |
| §07 只有月抄的站：标「月频口径」，不与日频站混排 | **保留** | |
| §07 上一年无抄表：同比位显「—」，不显 0% | **保留**（A2「去年无抄表」） | 0% 意味着持平 |
| §07 同比跨年月份不齐：按两年都有抄表的月对齐再比，报出参与月数 | **保留** | |
| §07 整年无抄表：整屏 `AnaEmpty` 深链 `/pv-income` | **保留** | |
| §07 第一年无月度基准：不画基准线，写「本年无月度绝对判据」 | **保留** | |

计数：**作废 37 条，保留 69 条。**

---

## §1 视觉词汇

### 1.1 来源文件（原样抄，不自创）

| 文件 | 取什么 |
|---|---|
| `frontend/src/styles/tokens.css` | 全部令牌（色 / 字 / 圆角 / 阴影 / 动效） |
| `frontend/src/components/ana/AnaKpiTile.vue` | KPI 瓦：标签 11 / 数值 16 mono 600 tabular / 副行 11 mono / 56×20 迷你趋势线 |
| `frontend/src/components/ana/ana.css` | `.av2-kpis`、`.av2-grid`（12 栏 gap 8）、`.av2-card`、`.av2-card-h .t / .hint`、`.ana-read`、`.ana-ref`、`.cz-tip`（气泡） |
| `frontend/src/components/ana/anaTheme.ts` | `FP_ANA_THEME`（网格 `rgba(28,28,28,.1)`、轴字 11px `rgba(28,28,28,.62)`、tooltip 深底）；`CAT_COLORS` |
| `frontend/src/components/ana/AnaForecastChart.vue` | 09-12 自绘：画布宽 = 实测宽（ResizeObserver）、只画横网格、悬停取最近刻度、气泡右侧放不下翻左 |
| `frontend/src/components/ana/AnaRentBandChart.vue` | 09-12 自绘：带 + 线 + 直标线尾数、悬停竖线 + 深底气泡 |
| `frontend/src/components/ana/AnaRenewalChart.vue` | 09-12 自绘：段内装不下字时放段外 |
| `frontend/src/components/ana/AnaUnitRentHist.vue` | 09-12 自绘直方图 |
| `../运维文档/设计稿/未实现/全站动效-2026-09-05/ChartScreen.dc.html` | 分析屏外壳（IconRail / SidebarPanel / TabStrip / Toolbar / AnaShell 工具条） |

### 1.2 色板

| hex | 编码什么 | 用在哪些块 |
|---|---|---|
| `#378ADD` | 主序列 / 选中栋 / 一期 / 自己用了 / 残差「多发」色阶 / 当段月 | B1 线、B3 选中栋线、A2 一期条、B7·B8 自用段、L1 一期条、L3 正向格、L2 柱、B9 趋势线、B11 线与点、L6 全齐格底（16%）、芯片期别点 |
| `#85B7EB` | 全园中位 / 卖上网 / 带中心线 / 淡带里的柱 | B3 全园中位线、B1 带中心虚线、B7·B8 上网段、L2 带内柱、B10 中线 |
| `#B5D4F4` | 各种「带」：这栋的范围 / 各栋中间一半 / 常态可能落的范围 / 平时的起伏 / 分布柱 | B1 带（40%）、B3 中间一半（45%）、B9 估计范围（50%）、B10 内带（50%）外带（25%）、L2 淡带（20%）、L4 直方柱（85%）、B6 右栏点图例 |
| `#5DCAA5` | 二期 | A2 条、L1 条、L3·L7·B8 期别点、芯片期别点 |
| `#EF9F27` | 三期 / 高于上沿 / 缺抄 / 损耗率 / 今年少了 / 残差「少发」色阶 / 超出内带 | B1 高于点与段、缺抄刻度；A2 三期条与 ▼；B7 损耗率线；L3 负向格；L6 缺抄格（30%）与缺抄榜条；B10 超内带点；芯片命中底（10%） |
| `#E24B4A` | 低于下沿 / 变点 / 观测值 / 超出外带（全屏唯一的负向与越界色） | B1 低于点与段、芯片命中底（10%）、B9 变点线、B10 超外带点、L4 观测线、L7 出范围色条与变点日期 |
| `#854F0B` | 琥珀底上的字、警示副行 | KPI 副行、A2 ▼ 后的 Δ、B7 右轴字、L4「比它更极端的只有…」、L6「缺 N 栋」 |
| `var(--hue-green)` | 今年多了 | A2 ▲ |
| `rgba(28,28,28,.45)` 虚线 | 参照线：锚点 807.5 h / =1 / 两者相等（`--ink-500` 口径） | A2 0 线、B11「1 = 与全园同步」、B6 对角线（.35） |
| `rgba(28,28,28,.30)` | 损耗（废掉的那部分） | B7 顶段 |
| `rgba(28,28,28,.22~.28)` | 群体散点 | B9 每日偏离、B10 在内带的点 |
| `rgba(28,28,28,.10)` + 45° 划痕 | 整日剔除 | L6 |
| `rgba(28,28,28,.03)` | 还没到的日子 | B1、B3、B7、B9、B10 右侧淡底 |
| `rgba(55,138,221,.07~.10)` | 当前期间 / 当段 | B9 当前期间底、B11 当段月底 |
| `rgb(40,52,66)` | 悬停气泡底 | 全部带悬停的块 |
| `var(--control-solid)` | 选中的芯片实底（白字） | B1 芯片 |

**两条定下来的配色决定：**
- **锚点线改墨阶虚线**（`rgba(28,28,28,.45)`），不用橙。橙让给三期与损耗 / 缺抄告警 —— 画板早期 B5 的橙锚点线与三期橙撞色。
- **期别三色只在「一栋一条 / 一栋一点」的图上出现**（A2、L1、L3、L7、B8、芯片点）；三期橙在本屏只落在未投产的两栋上。

### 1.3 字

| 字号 | 用在 |
|---|---|
| 16 | KPI 数值（mono 600）、抽屉头栋名、侧栏层名 |
| 14 | 卡头标题（600）、段控字（500）、大图图头栋名（600）、面包屑 |
| 12 | 正文、`.ana-read` 读数句、栋名、表格正文 |
| 11 | hint、轴字、图注 `.ana-ref`、芯片、徽标、KPI 标签与副行、气泡 |

- 数值一律 `--font-mono` + `tabular-nums`；中文走 `--font-sans`。
- 本屏不用 20 / 15 / 26。字重 400 / 500 / 600。

### 1.4 卡片

- `.av2-card`：白底、1px `--border-subtle`、圆角 8、内边距 10 / 12。
- 卡头 `.av2-card-h`：标题 14px 600 + hint 11px muted，下边距 8；卡角徽标 11px、高 18、999 圆角、`--surface-sunken` 底。
- 读数句 `.ana-read` 12px 墨色；参照系小字 `.ana-ref` 11px muted。

### 1.5 悬停气泡（全屏一套）

| 项 | 值 |
|---|---|
| 底 / 圆角 / 内边距 | `rgb(40,52,66)` / 9 / 8×11 |
| 阴影 | `0 8px 24px rgba(0,0,0,.18)` |
| 字 | 11px mono tabular；首行白 600，其余行 400 |
| 行色 | 选中栋 `#8FC2F2`、在范围内 / 多了 `#9EDCC6`、高于 / 少了 / 缺抄 `#F6C77A`、低于 `#F58B8A`、次要 `rgba(255,255,255,.62~.82)` |
| 位置 | 竖线右侧 12px；右侧放不下翻到左侧；行命中类气泡：上半行在行下、下半行在行上 |
| 竖线 / 高亮点 | 1px `rgba(55,138,221,.45)`；高亮点 r5 实心 `#378ADD` + 2px 白环 |
| 命中判据 | 逐刻度图取 x 最近的刻度，不要求对准点；行类图整行命中 |

---

## §2 页面结构

宽度口径：1440 视口下主卡片 1051、卡内 1025；1366 视口下主卡片 977、卡内 951。**所有自绘画布宽度 = 卡内实测宽（ResizeObserver），高度固定**；画板上写死的 1025 / 999 是 1440 下的实测值。

### 2.1 首屏（段控以上）

| 块 | 栅格 | 卡高 |
|---|---|---|
| KPI 行（6 × `AnaKpiTile`） | `.anx-kpis` 六等分 | 94（含容器上内边距 12） |
| `.anx-body` 上内边距 | — | 24 |
| B1 主卡（芯片条 + 通栏大图 + B2 判据脚） | s12 | 386（恒定，不随任何操作变） |
| gap | — | 12 |
| 段控 | — | 32 |

**1366×768 首屏预算（重算）**

| y | 块 | 高 |
|---|---|---|
| 0 | KPI 行 | 94 |
| 94 | body 上内边距 | 24 |
| 118 | B1 主卡 | 386 |
| 504 | gap | 12 |
| 516 | 段控 | 32 |
| | **合计** | **548** |

- 可用高：视口 ≈ 620 − 外壳 158（页签条 44 + 工具栏 48 + AnaShell 工具条 53 + stage 上内边距 12 + 卡边 1）= **462**。（v3 写 157 / 463，本次按画板实测 158。）
- **超预算 86px**：主卡下沿在 y=504，比折线低 42px —— 判据脚被切、段控整条在折线下。怎么办见 §4。

### 2.2 「绝对水平」档

| 块 | 栅格 | 卡高 |
|---|---|---|
| B3 等效小时轨迹 | s12 | 316 |
| A2 年等效小时 | s12 | 485 |
| B6 台账装机 vs 板数 × 标称 | s12 | 383 |
| **合计（含两道 gap 8）** | | **1200** |

### 2.3 「账面量」档（默认档）

| 块 | 栅格 | 卡高 |
|---|---|---|
| B7 消纳结构与损耗率 | s12 | 361 |
| B8 各栋消纳收益与上网收益 | s12 | 472 |
| **合计（含 gap 8）** | | **841** |

- 段控容器 min-height 按两档较高者钉 **1200**（§6.5 保留规则）；停在「账面量」时下方留 359 空白。

### 2.4 「高级分析」档

| 块 | 栅格 | 卡高（1440 / 1366） |
|---|---|---|
| L1 先天水平 α 排序（含 L5 一行） | s6 | 457 / 480 |
| L3 各栋残差的年内走势 | s6 | 同行拉伸 457 / 480 |
| L6 数据质量日历 | s8 | 572 / 587 |
| 右栏：L2 昨天偏高 + L4 偏差难碰巧（一个 s4 栏里上下叠两张卡，gap 8） | s4 | 282 + 8 + 282 = 572 |
| L7 逐栋核对表 | s12 | 521 |
| **合计（含两道行 gap 8，1440）** | | **1566** |

- L2 / L4 叠在一个 s4 栏里而不是两个独立格子：s8 + s4 + s4 + s12 在 12 栏里怎么排都会留一个 8 栏空洞；叠放后与 L6 等高。
- 不抬段控容器 min-height（§6.5b 保留规则）。

### 2.5 单栋抽屉

| 块 | 栅格 | 卡高 |
|---|---|---|
| 抽屉头（栋名 + 「整年在网 N 天 · 年发电 X 万度 · 装机 · 期别」 + 上一栋 / 下一栋 / 关） | 720 覆盖层 | 51 |
| B9 这一年的偏离与水平变化 | 672 | 389 |
| B10 逐日偏离与两道范围线 | 672 | 354 |
| B11 跟全园一起涨落的程度 | 672 | 281 |
| B12 逐刻度明细 | 672 | 346 |
| **抽屉合计**（内边距 24、卡间 gap 8） | | **1495** |

---

## §3 逐块规格

规矩：
- 表里写着「画板示意值」的数字，是画板为了画出形状编的，**不是关于真实库的事实**，不许抄上屏。dev 库的光伏抄表由 `PvMeterService.simulate` 生成（PV-ANALYSIS-SPEC §08 前置）。
- 「取数」写的函数 / 字段都在 `frontend/src/views/analysis/pvMeterAna.logic.ts`，接口 `GET /api/pv-meter/readings?year=`（`pv_reading` 表），参数读 `snap.crit`（V122 / V123）。
- 共 20 块：首屏 4（B0 / B1 / B2 / 段控）、绝对水平 3（B3 / A2 / B6）、账面量 2（B7 / B8）、高级分析 7（L1 / L5 / L3 / L6 / L2 / L4 / L7）、抽屉 4（B9–B12）。

### §3.1 B0 · KPI 行

| 项 | 内容 |
|---|---|
| 卡头 / hint | 无卡头。六瓦标签逐字：「本段出范围栋数」「读不出」「未装表」「已录板数」「缺抄条数」「数据到」 |
| 图种 | DOM，现成 `AnaKpiTile` × 6；「本段出范围栋数」「缺抄条数」两瓦带 56×20 迷你趋势线 |
| 几何 | `.av2-kpis` 六等分 gap 8；瓦内 标签 11 / 数值 16 mono 600 / 副行 11 mono；行高 94（含上内边距 12），恒高 |
| 颜色编码 | 数值墨色；副行在「出范围栋数 > 0」「缺抄条数 > 0」时 `#854F0B`，为 0 或其余瓦 muted；迷你线 muted 1.2px |
| 交互 | 无悬停、无点击 |
| 取数 | 出范围栋数 = `snap.board` 里 `runs.length > 0` 的栋数（不数点）；读不出 = 已抄 ÷ `elapsedN` < `crit.coverMonth` 或在网 < `crit.minOnlineDays`；未装表 = `snap.quality.noMeter`；已录板数 = 总栋数 − `quality.noPanel`；缺抄条数 = Σ(`elapsedN` − `seenN`)；数据到 = `snap.dataThrough`，已过去 = `elapsedN` / `ticks.length`。**画板示意值**：「2 栋 / E座 · F座」「0 栋 / 11 栋覆盖都 ≥ 60%」「2 栋 / 创业大厦 · 工业大厦」「11/13 / 未投产 2 栋不计」「3 条 / E座 2 · G座 1」「08-28 / 已过去 28/31 天」；其中「≥ 60%」与参数定值 90% 不符，「已录板数 11/13」与 B6「13 栋板数尚未录入」自相矛盾。两条迷你线的序列是编的，库里没有逐月「出范围栋数 / 缺抄条数」序列（见 §4） |

### §3.2 B1 · 哪栋落在自己的范围外（主卡）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「哪栋落在自己的范围外」/「芯片 = 一栋，徽标 = 出范围天数 · 点芯片换图 · 线 = 这栋当日发电 ÷ 全园同日中位，带 = 这栋自己的范围」；卡头右侧链接「看 X 的整年 →」；大图图例逐字「逐日比值 / 这栋的范围 / 低于下沿 / 高于上沿 / 缺抄」 |
| 图种 | 芯片条 = DOM（`AnaPill` 口味）；大图 = **自绘 SVG**。ECharts 画不出的：§3.8 三态要逐刻度分画（漏抄断线 + 底部缺口刻度 / 未到淡底）；连续段底色要夹进绘图区；上下沿数值要贴带边直标；`echartsBundle.ts` 未注册 `custom` / `graphic` |
| 几何 | 芯片行高 34（芯片高 26、999 圆角、11px、gap 6，一行 13 枚）；大图 = 图头 24 + 画布 236（padL 44 / padR 14 / padT 12 / padB 24），宽 = 卡内宽；x = 当月 1…31 日画满、标 1 / 5 / 10 / 15 / 20 / 25 / 31；y 按选中栋自适应 + 12% 余量、4 条横网格；主卡恒高 386 |
| 颜色编码 | 线 `#378ADD` 2px；带 `#B5D4F4` 40%；中心虚线 `#85B7EB`；低于下沿点 `#E24B4A` / 高于上沿点 `#EF9F27`（r4、1.5px 白环）；连续段底色同色 10%；缺抄 `#EF9F27` 2×7 刻度；未到淡底。芯片：命中栋按主导方向（低于多 → 红，高于多 → 琥珀）10% 底 + 同色字 + 徽标「N 天」；未命中白底 1px 边；选中 `--control-solid` 实底白字 600；未投产灰字、点灰 |
| 交互 | 点芯片 = 换大图（不开抽屉）；未投产芯片不可点；大图悬停：竖线 + 高亮点 + 气泡四行「8 月 D 日 / 比值 x.xxx / 范围 lo – hi / 在范围内｜低于下沿｜高于上沿」，缺抄日第二行「这天没抄表」；卡头链接开抽屉 |
| 取数 | 芯片顺序 = 有连续段在前，再按出范围天数降序（排序键 = 徽标）；比值 `BoardRow.ratio`、三态 `state`、带 `lo / hi / center`、出范围 `out`、段 `runs`；图头事实句三段式由 `outN / runs / elapsedN − seenN` 拼。**画板示意值**：11 栋 × 31 天形状取自 `../运维文档/设计稿/被否/光伏分栋分析第一版-2026-09-13/context/v3-design-2026-09-02.html` 的 `BOARD`；缺抄日（E座 25 / 26、G座 14）、「数据到 28 日」、默认选中 F座 都是板上设定 |

### §3.3 B2 · 判据脚

| 项 | 内容 |
|---|---|
| 卡头 / hint | 无卡头（主卡页脚单行）。逐字：「范围 = 这栋自己的水平 ± 2 倍稳健波动 · 连续 ≥ 3 天出范围计一段 · 抄表覆盖 ≥ 60% 才判 · 台账差 ±3% · 年等效 ≥ 807.5 h · 去改」 |
| 图种 | DOM 文本，非图 |
| 几何 | 上 1px 分隔线（上 8 下 6）+ 一行 11px mono，flex-wrap，gap 2 / 14 |
| 颜色编码 | muted；「去改」`--text-link` |
| 交互 | 「去改」深链参数中心；年档写「连续 ≥ 3 个月出范围计一段」，不划掉（2026-09-13 用户拍板） |
| 取数 | `snap.crit`：`bandSigma`（pv_band_sigma）/ `bandRun`（pv_band_run）/ `coverMonth`（pv_crit_cover_month）/ `ledger`（pv_crit_ledger）/ `anchorHours × yieldRatio`（pv_yield_anchor_h × pv_crit_yield_ratio）。**画板示意值**：「≥ 60%」是错的，参数定值 90%；「807.5 h」= 950 × 0.85 与参数定值一致 |

### §3.4 段控

| 项 | 内容 |
|---|---|
| 卡头 / hint | 无卡头。三档逐字「绝对水平 / 账面量 / 高级分析」；右侧说明句逐字「「高级分析」= 算法自检与口径核对，给要复算这屏数字的人；含 p / q / σ 等统计量，其余各档不出现。」 |
| 图种 | DOM，ds Segmented md 32，非图 |
| 几何 | 高 32；下方容器 min-height 钉 1200（§2.3） |
| 颜色编码 | `--surface-sunken` 槽；选中白底 + `--shadow-pill`、墨色；未选 muted |
| 交互 | 切档不换卡；默认「账面量」（画板停在「高级分析」只为展示）；「高级分析」要点才算 |
| 取数 | 无；说明句里的统计术语待定（§4） |

### §3.5 B3 · 等效小时轨迹（绝对水平档）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「等效小时轨迹」/「每天每千瓦装机发了多少度，这栋和全园比」；线尾直标逐字「各栋中间一半 / 全园中位 / F座」；图注逐字「纵轴 = 等效小时 kWh/kWp · 横轴 = 2025 年 8 月 1…31 日 · 11 栋在网、2 栋未投产 · 数据到 28 日，右侧淡区还没到」 |
| 图种 | **自绘 SVG**。ECharts 画不出的：三条线尾直标要互相避让（bundle 未引 `echarts/features` 的 LabelLayout）；带用 `bandSeries` 合成时两条系列按注释不进 tooltip，气泡里的「中间一半 lo – hi」得另写 formatter；与 B1 共用一套几何与悬停 |
| 几何 | 画布 卡内宽 × 250（padL 44 / padR 62 / padT 12 / padB 24）；x 同 B1；y 自适应 + 18% 余量、4 条横网格；线尾标签最小间距 13 |
| 颜色编码 | `#B5D4F4` 45% = 各栋当日中间一半（p25–p75）；`#85B7EB` 2px = 全园中位；`#378ADD` 2.5px = 选中栋；无图例 |
| 交互 | 悬停：竖线 + 选中栋高亮点 + 气泡四行「8 月 D 日 / F座 x.xx h / 全园中位 x.xx h / 中间一半 lo – hi h」，缺抄日第二行「F座 这天没抄表」；选中栋跟 B1 芯片走 |
| 取数 | 当日等效小时 = 当日 `gen` ÷ 分母（`theoKwp` 优先、退 `capKwp`，§3.5）；在网 < 3 栋的刻度留空；全园线漏抄日按当天在网栋算。**画板示意值**：逐日等效小时是「年等效 ÷ 365 × 8 月季节系数 × 当日相对月均偏离」合成的（生成器 `lib.mjs` 的 `ehOf`），库里没有这条口径；纵轴 1.9–4.7 |

### §3.6 A2 · 年等效小时（绝对水平档，替掉 B4 + B5）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「年等效小时」/「每千瓦装机一年发了多少度，比标杆多多少」；卡角徽标「整年口径 · 与 2024 比」；图注逐字「横轴不从 0 h 起：0 就是 807.5 h 那条线，条长只代表离标杆多远，不能照着长度算年等效的倍数 · 锚点线用墨阶虚线，橙留给三期 · 最右一列 ▲▼ = 比 2024 年多 / 少 · 去年无抄表 2 栋（12栋、13栋）那一列空着」 |
| 图种 | **自绘 SVG**。ECharts 画不出的：条尾两枚标签（`+x h` 与 `+z%`）+ 最右一列不属于任何轴的 ▲▼Δ；0 线偏左 22% 的非对称横轴；未投产行占行写字不画条 |
| 几何 | 画布 卡内宽 × 400（13 行 × 26 + 上 8 下 34）；栋名右对齐于 x=82（padL 92）；0 线 = padL + 22% 绘图宽；右侧预留 176；条高 14、圆角 3；每 +50 h 一条竖网格 |
| 颜色编码 | 条按期别三色（`fill-opacity .85`）；选中栋同色不透明 + 1.5px 墨描边（不整条变黑）；0 线墨阶虚线；▲ `var(--hue-green)` / ▼ `#EF9F27`、▼ 后 Δ 字 `#854F0B`；图例逐字「一期 / 二期 / 三期 / 0 = 807.5 h / 条长 = 比 807.5 h 多多少小时」 |
| 交互 | 整行命中 → 行底 5% 墨 + 气泡四行「栋名 · 期别 / 年等效 x h / 比锚点多 y h（+z%）/ 比去年多｜少 w h」，去年无抄表第四行「去年无抄表，比不了」；无点击（抽屉入口待定，§4） |
| 取数 | 年等效 = `StationRow.yieldHours`（分母 `yieldDenom`）；锚点 = `crit.anchorHours × crit.yieldRatio`；比去年 = 按两年都有抄表的月对齐（§07），上一年无抄表显「去年无抄表」；在网 < 90 天移出并图注计数，不年化。**画板示意值**：877–1147 h、各栋 2024 值（生成器 `YH24`）、「去年无抄表 12栋 13栋」 |

### §3.7 B6 · 台账装机 vs 板数 × 标称（绝对水平档）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「台账装机 vs 板数 × 标称」/「台账上写的装机，和现场板子算出来的对不对得上」；右栏逐字：灰底提示「13 栋板数尚未录入，此为录入后的样子。点画成淡色、不标栋名，因为现在没有一栋是真数。」；三条图例「虚线 = 两者相等：台账上写的装机，正好等于 板数 × 单块标称 ÷ 1000。点落在线上就是对得上。」「灰带 = 相差 ±3% 以内：算对得上。落在带外面的栋，台账和现场有一边写错了。」「一个点 = 一栋：横轴是现场板子算出来的，纵轴是台账写的，两轴同一把刻度，单位 kWp。」；链接「去楼栋档案录入板数与单块标称功率 →」 |
| 图种 | **自绘 SVG**。ECharts 画不出的：±3% 是沿 45° 对角线的斜带，`markArea` 只能画轴对齐矩形，斜多边形要 `custom` / `graphic`，bundle 均未注册 |
| 几何 | s12 卡；左 300×300 正方绘图区（+34 轴字），两轴同 min / max；右栏 ≤ 460 宽、gap 32 |
| 颜色编码 | 对角线墨阶虚线 .35；±3% 带墨 5%；点 `#378ADD` 22% 填 + 35% 描边、不标栋名 |
| 交互 | 画板无悬停（示意态没有真数可读）；右栏链接跳楼栋档案 |
| 取数 | x = `theoreticalKwp`（`panel_count × panel_watt ÷ 1000`，V121）；y = `capacity_kwp`；未录的站不画点、图注「n 栋未录」（§07）。**画板示意值**：11 个点的 x 是台账值 ±5% 编的；PV-ANALYSIS-SPEC §01 记 13 站板数全空（本文未核库），照那条真屏今天画不出一个点，空态画法待定（§4） |

### §3.8 B7 · 消纳结构与损耗率（账面量档）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「消纳结构与损耗率」/「每天发的电，多少自己用了、多少卖上网、多少路上损掉了」；图例逐字「自己用了 / 卖上网 / 路上损掉 / 损耗率（右轴）」；图注逐字「左轴 = 万度，三段自下而上 = 自己用了 / 卖上网 / 路上损掉 · 右轴 = 损耗率，刻度钉死在 0–6% 不随数据缩放 · 数据到 28 日」 |
| 图种 | **自绘 SVG**（用户对照后选择保持现状）。若改 ECharts：副轴溢出时「轴外三角 + 数值」要 `graphic`，bundle 未注册 |
| 几何 | 画布 卡内宽 × 272（padL 46 / padR 46 / padT 22 / padB 26）；31 个槽，柱宽 = 槽宽 × 0.6；左轴 万度 4 格；右轴 0 / 2 / 4 / 6% |
| 颜色编码 | 自用 `#378ADD` / 上网 `#85B7EB` / 损耗 墨 30%（只顶段 3px 圆角）；损耗率 `#EF9F27` 1.5px + 白芯点 r2.5；右轴字 `#854F0B`；29–31 日淡底 |
| 交互 | 悬停 → 当日柱槽 5% 墨 + 气泡五行「8 月 D 日 / 自己用了 x 万度 / 卖上网 x 万度 / 路上损掉 x 万度 / 损耗率 x%」（≥ 3% 那行字转 `#F6C77A`） |
| 取数 | `snap.ledger.self / grid / loss`（与 ticks 等长：月段逐日、年段逐月）、`ledger.lossPct`；右轴固定 0–6%。**画板示意值**：逐日自用率与损耗率是生成器函数 `selfRate / lossRate` 造的，17 日 4.9% 是特意放的尖峰 |

### §3.9 B8 · 各栋消纳收益与上网收益（账面量档）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「各栋消纳收益与上网收益」/「这个月每栋一共挣了多少钱，自己用的和卖上网的各占多少」；图例逐字「自己用的（按录入时的单价 ¥0.52/度）/ 卖上网的（按参数里的标杆价 ¥0.391/度）/ 一期 / 二期 / 三期 / 条尾粗字 = 合计万元」；图注逐字「横轴 = 万元，从 0 起 · 两段叠起来，条尾就是合计，不用心算 · 按合计从多到少排 · 两段单价口径不同（自用按录入时的单价、上网按参数标杆价）· 数据到 28 日」 |
| 图种 | **自绘 SVG** 堆叠横条。ECharts 能画（stack bar + label formatter），选自绘是为了与同档 B7 共用一套悬停 / 气泡与字宽度量（段内写数要按像素宽判断） |
| 几何 | 画布 卡内宽 × 383（13 行 × 27 + 上 6 下 26）；栋名列 96（期别点 x=10、栋名 x=22）；条高 16、圆角 3；段内写数门槛：段宽 ≥ 38px；条尾合计右留 84；横轴整数刻度 |
| 颜色编码 | 自用段 `#378ADD`（段内白字）/ 上网段 `#85B7EB`（段内 72% 墨字）/ 合计墨色 600；期别点三色 |
| 交互 | 整行命中 → 行底 4% 墨 + 气泡五行「栋名 · 期别 / 本段发电 x 万度 / 自己用了 ¥x 万 / 卖上网 ¥x 万 / 合计 ¥x 万」 |
| 取数 | `StationRow.revSelf`（`self_use × price_snap`，调价不改历史）/ `revGrid`（`grid_feed ×` 参数标杆价 `gridPrice`）/ `genYear`；**累计口径**：月中标题改「截至 M/D 累计」、提示「不可与整月直接比」（§6.5）。**画板示意值**：各栋自用占比（生成器 `selfB`）、¥0.52 单价、合计 1.50–3.95 万元；「1025 宽下 13 栋两段数值都写得下」是这组示意值下的画板实测，真数下要重算 |

### §3.10 L1 · 先天水平 α 排序（高级分析档）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「先天水平 α 排序」/「条 = 这栋常年高于 / 低于全园中位的程度 · 淡带 = 90% 区间 · 点条换上面的图」；图例逐字「一期 / 二期 / 三期 / 横轴 = α，相对全园中位 %」；参照系逐字「n = 11 栋 · 全年逐日残差 · 相对全园中位，% · 带 = 块自助 90% 区间」 |
| 图种 | **自绘 DOM 横条**（用户明确「保持现在这样」）。ECharts 横向 bar 可画，但「同色 30% 延长带 + 正负值标签放两侧 + 未投产占行」要拼三层 series，形态已定，没有收益 |
| 几何 | 画布 卡内宽 × 300；13 行 × 22；栋名列 58 右对齐；条高 14、圆角 3；0 线墨 35%；每 10% 一条竖网格 |
| 颜色编码 | 条按期别三色；区间 = 同色 30% 延长带；选中栋 1.5px 墨描边 + 栋名 600 |
| 交互 | 点条 = 换 B1 大图（与芯片共用一个选中态）；无悬停 |
| 取数 | `lab.alphaRows`（`alphaPct / ciLo / ciHi`，按 α 排）；台账差超线被踢出的栋必须写名单 `lab.alphaExcluded`；读数句 `.ana-read` 由选中栋拼（「F座 α −38.0%，11 栋里第 11 位（1 = 最高）；区间 … ，不跨 0」）。**画板示意值**：α 取 `BOARD.dev`、区间半宽由带宽反推。hint 写「90% 区间」，`congenitalCheck` 实算 2.5% / 97.5%（见 §4） |

### §3.11 L5 · 这份排名换一种算法顺序重算了一遍（高级分析档，α 卡脚注行）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 无卡头（α 卡脚注区一行 `.ana-read` 12px，在 α 读数句与参照系小字之间）。逐字：「这份排名换一种算法顺序重算了一遍：11 栋里 9 栋名次没变，2 栋各挪了 2 位（10栋 4→6 · G座 10→8）。」 |
| 图种 | 一句话（2026-09-13 用户看真屏后要求删掉原来右侧的 88×20 迷你收敛线：没轴没标签，不悬停读不出东西） |
| 几何 | 与 α 卡同宽一行，句子可折两行 |
| 颜色编码 | 句子墨色 |
| 交互 | 无 |
| 取数 | 句子 = `lab.convergence.rowRank / colRank`，名次只在进 α 排序的栋里重排（在网不足 `crit.minOnlineDays` 天的不算） |

### §3.12 L3 · 各栋残差的年内走势（高级分析档）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「各栋残差的年内走势」/「格 = 该月残差中位数 % · 蓝 = 比自己常年多发，琥珀 = 少发 · 悬停看数」；图例逐字「少发 … 多发」九格色阶、右侧「9–12 月还没到 · 未投产 2 栋无残差」 |
| 图种 | **CSS Grid 手写热力格**（bundle 无 `HeatmapChart` / `VisualMapComponent`，§6.5 已写） |
| 几何 | 13 行 × 12 列；格 28×28、圆角 4、gap 2；行头 70（期别点 + 栋名）；列头 20，当段月列头 accent 实底白字；未到的月与未投产行虚线空格 |
| 颜色编码 | 正 = `rgba(55,138,221, .14 / .30 / .50 / .75)`，负 = `rgba(239,159,39, 同四档)`，接近 0 = 墨 4%；最深一档正向格字转白 |
| 交互 | 格悬停 → 墨 2px 内描边 + 气泡三行「栋名 · 2025-0M / 残差 ±x% / 比自己常年水平多发｜少发｜持平」 |
| 取数 | `lab.season[].months`（对数残差月中位数，屏上换算 (eᵛ − 1) × 100%）；有效抄表不足的月为 null → 空格不补；`seasonPartial` 月要标出（画板未画）；色阶档位由 `seasonHalf` 定（画板用 2 / 4 / 7 / 10%）。**画板示意值**：格内全部数值（生成器 `resid()` 造）；行序（见 §4） |

### §3.13 L6 · 数据质量日历（高级分析档）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「数据质量日历」/「哪一天、哪几栋没抄表」；缺抄榜表头逐字「这一段谁漏抄了 / 13 栋全列」；图例逐字「11 栋全抄了 / 有栋没抄 / 整日剔除 / 还没到 / 2025 年 8 月」 |
| 图种 | **CSS Grid 手写日历 + DOM 缺抄榜**（同 L3，bundle 无 heatmap） |
| 几何 | s8 卡；左日历 行 = 周一…周日、列 = 周（月档 5–6 列），格 59×64、圆角 5、gap 4，列头「第 N 周」、行头周内日；右缺抄榜 300 宽、13 行 × 22（期别点 + 栋名 64 + 琥珀零基条 + 「缺 d 天」56 右对齐） |
| 颜色编码 | 全齐 `rgba(55,138,221,.16)` + 格内灰字「11 栋全齐」/ 缺抄 `rgba(239,159,39,.30)` + `#854F0B`「缺 N 栋」/ 整日剔除 墨 10% + 45° 划痕 + 「整日剔除」/ 还没到 白底虚线框；榜条 `#EF9F27`；未投产行灰字「未投产」无条 |
| 交互 | 格悬停 → 墨 2px 内描边 + 气泡两行「8 月 D 日 / 缺抄 N 栋 · 名单｜整日剔除 · 全园当天读数不进判定｜11 栋都抄齐了｜还没到」 |
| 取数 | `lab.quality.rows[].states`（`ok / missing / dropped / pre`）按日聚合；整日剔除 = 当天进矩阵栋数 < `snap.quality.minStations`（3）；「当天抄了几栋」必须取 `quality.onDay`，不许数 cells（logic 注释：数出来的是在产栋数）；月档按自然月铺满。**画板示意值**：14 / 25 / 26 日缺抄、17 日整日剔除、缺抄榜 E座 2 天 · G座 1 天 |

### §3.14 L2 · 昨天偏高，今天还会偏高吗（高级分析档）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「昨天偏高，今天还会偏高吗」/「偏差是一天天连着的，还是各天各的」；读数句逐字「隔 1 天还剩 0.62，隔 3 天就掉到 0.28 —— 前一天偏高，第二天多半还偏高。」；参照系逐字「横轴 = 隔几天 · 淡带之内算没有规律，柱子画浅一档 · 拿这一栋整年的逐日偏差算」 |
| 图种 | **自绘 SVG 竖柱**。ECharts bar + markArea 能画；选自绘为与同栏 L4 同一套柱形、悬停与气泡 |
| 几何 | 画布 卡内宽 × 138（1440 下 312）；x = 隔几天，标 1 / 4 / 7 / 10 / 14；y 网格 0 / 0.3 / 0.6；柱宽 = 槽宽 × 0.56、顶圆角 2 |
| 颜色编码 | 柱 `#378ADD`；落在淡带之内的柱 `#85B7EB`；淡带 `#B5D4F4` 20% |
| 交互 | 柱悬停 → 柱槽 5% 墨 + 气泡两行「隔 k 天 / 值 + 『在淡带里，算没有规律』或『还看得出关系』」 |
| 取数 | `lab.acf[].rho`（选中栋）；滞后上限 `min(30, ⌊n/4⌋)`，不许写死。**画板示意值**：14 个相关值、读数句里的 0.62 / 0.28、淡带阈值 0.13（现 `PvMeterAnaView` 的 L2 没有这条带，阈值口径见 §4） |

### §3.15 L4 · 这个偏差有多难碰巧出现（高级分析档）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「这个偏差有多难碰巧出现」/「把这一栋的日子打乱重算 1000 遍，看能不能碰出这么大的偏差」；线上直标逐字「这一段 +0.062」「比它更极端的只有 7/1000 次」；读数句逐字「1000 遍里只有 7 遍比这一段更偏 —— 这个偏差不像是碰巧碰上的。」；参照系逐字「横轴 = 打乱之后算出来的偏差 · 柱高 = 1000 遍里落在这一格的次数 · 红线 = 这一段真实的偏差」 |
| 图种 | **自绘 SVG 直方图 + 观测竖线**（09-12 `AnaUnitRentHist` 同笔法） |
| 几何 | 画布 卡内宽 × 138；16 格；观测线 2px，上端伸出绘图区 4px；线左直标两行 |
| 颜色编码 | 柱 `#B5D4F4` 85%；观测线 `#E24B4A`；「这一段 +x」`#E24B4A` 600；「比它更极端的只有 N/1000 次」`#854F0B` |
| 交互 | 柱悬停 → 格 5% 墨 + 气泡两行「lo ~ hi / 1000 遍里落在这一格 c 遍」 |
| 取数 | `lab.nullDist.dist / obs`（跟选中栋走，不给取 p 最小那栋）；尾概率从 `dist` 现算印在线上；「1000 遍」= B + 1（B = 999，§3.3）；观测窗口跟显示段、退回要说出来。**画板示意值**：分布形状、+0.062、7/1000 |

### §3.16 L7 · 逐栋核对表（高级分析档）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「逐栋核对表」/「上面那几张图的数，一栋一行摊开对」；表头逐字「楼栋 / 期别 / 常年水平 / 名次 / 区间 / 哪天起变了 / 有效月数」；表脚逐字「名次 1 = 常年水平最高 · 左侧色条 = 本段有连续出范围的栋（红 = 低于，琥珀 = 高于）· 砍掉了 6 列算法中间量（检验用的统计量、重算了多少遍、每次取多长、有效天数等）：那些是要复算这屏数字才用得上的，导出的 CSV 里有」 |
| 图种 | HTML 表 |
| 几何 | 7 列：楼栋 96 / 期别 64 / 常年水平 104 / 名次 64 / 区间 168 / 哪天起变了 120 / 有效月数 96；行高 32；表头 11px 600、sticky；13 行（未投产 2 行合并单元格灰字「未投产 · 没有可算的行」） |
| 颜色编码 | 出范围栋左 2px 色条（红 = 低于、琥珀 = 高于）；「哪天起变了」有值时 `#E24B4A`；数值 mono tabular |
| 交互 | 行 hover 底 `--surface-card`；无气泡、无点击 |
| 取数 | `lab.tests`（`alphaPct / cpRange / days`）+ `lab.alphaRows`（`ciLo / ciHi`）；名次按 α 降序；有效月数 = `lab.season[].months` 非 null 个数。**画板示意值**：变点日期（E座 6月9日、F座 3月2日、9栋 5月21日）、有效月数 7 / 8 与 8 / 8；表脚「导出的 CSV 里有」现无对应导出（见 §4） |

### §3.17 B9 · 这一年的偏离与水平变化（抽屉）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「这一年的偏离与水平变化」/「这一年里，这栋什么时候开始变了」；变点旁直标逐字「6月9日起 / 水平抬上去了」；当段标注「当前期间 8 月」；图例逐字「每天的偏离 / 这段时间的常态 / 常态可能落的范围 / 这天前后水平变了」；参照系逐字「纵轴 = 与全园同刻度中位的偏离（0 = 与全园同步）· 横轴 = 2025 年逐日 · 缺抄日断开不画 · 右侧淡区还没到」 |
| 图种 | **自绘 SVG**。与 B10 同一张画布几何（同一 x 映射、悬停取最近日），抽屉里上下对齐；ECharts 里变点竖线用 `markLine` 不参与轴范围，需手设 min / max（§6.5 注） |
| 几何 | 画布 646×300（padL 46 / padR 14 / padT 14 / padB 26）；x = 1…365 日、标 12 个月初；y 网格 −0.15 / 0 / 0.15 / 0.30 |
| 颜色编码 | 每日偏离 墨 22% r2；常态（平滑趋势）`#378ADD` 2px；估计范围 `#B5D4F4` 50%；变点 `#E24B4A` 1.5px 竖线 + 两行红字；当前期间 `rgba(55,138,221,.07)` 底 + 蓝字；数据截止后淡底 |
| 交互 | 悬停 → 竖线 + 最近一天高亮点 + 气泡三行「M月D日 / 偏离 ±x.xxx / 在 6 月 9 日水平变化之前｜之后」 |
| 取数 | `buildDetail(snap, id)`：`dates / resid` 散点、`spline`（`rcsTrend`，4 节点）趋势与带、`cpDate` 竖线、`cpLo / cpHi` 区间阴影（画板未画，见 §4）；固定按年；在网 < 8 天整块不出。**画板示意值**：抽屉看的是 E座；整年残差由生成器 `residOf()` 造；变点第 160 天（6月9日）；趋势画的是 ±12 天滑动中位而非 `rcsTrend` 样条 |

### §3.18 B10 · 逐日偏离与两道范围线（抽屉）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「逐日偏离与两道范围线」/「每天的偏差有没有超出它平时的起伏」；带标注逐字「平时的起伏 / 更宽的那道」；估计窗口直标逐字「这两道线是拿 1 月 1 日 – 6 月 8 日 这一段估的」；图例逐字「在里面 N 天 / 超出里面那道 N 天 / 超出外面那道 N 天 / 平时的起伏 / 更宽的那道」；参照系逐字「中线与两道范围都用 6 月 9 日水平变化之前那一段估，之后的日子拿来对照 —— 所以 6 月之后的点整体偏上，是这次变化本身，不是新的异常」 |
| 图种 | **自绘 SVG**（与 B9 同几何；ECharts `markArea` 能画两层带，选自绘为抽屉内两图对齐） |
| 几何 | 画布 646×250；x 同 B9；y 网格 −0.2 / 0 / 0.2；估计窗口底条高 10 |
| 颜色编码 | 中线 `#85B7EB` 1.5px；内带 `#B5D4F4` 50% / 外带 25%；在内 墨 28% r1.8 / 超内带 `#EF9F27` r2.8 / 超外带 `#E24B4A` r3；估计窗口 墨 10% 底条 |
| 交互 | 悬停 → 竖线 + 气泡三行「M月D日 / 偏差 ±x.xxx / 在里面｜超出里面那道｜超出外面那道」 |
| 取数 | `buildDetail`：`center`、`sigma`（内带 ± 2 倍、外带 ± 3 倍 —— 屏上不写 σ）、`limitFrom / limitTo`；变点前段 < 8 点退回全期并如实标；数据不够时 runs rule 不报。**画板示意值**：中心 −0.015、半宽单位 0.052、「176 / 35 / 27 天」三个计数 |

### §3.19 B11 · 跟全园一起涨落的程度（抽屉）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「跟全园一起涨落的程度」/「全园发得多的月份，这栋是不是也跟着多」；参照线标注逐字「1 = 与全园同步」；当段标注「当段」；空月刻度下逐字「不足 / 未到」；参照系逐字「12 个月的槽位都留着，没值的月不删只留空：投产前（本栋没有）· 样本不足（1 月抄表天数太少）· 还没到（9–12 月）· 大于 1 = 全园多发时它比全园还多」 |
| 图种 | **自绘 SVG 点线**（与 B9 / B10 同一套抽屉几何；12 个月槽固定，空槽要占位写原因） |
| 几何 | 画布 646×200（padL 46 / padR 14 / padT 16 / padB 30）；x 12 槽等分；y 网格 0.9 / 1.0 / 1.1 |
| 颜色编码 | 线 `#378ADD` 2px；点 r7 `#378ADD` + 2.5px 白环；=1 参照 `--ink-500` 虚线；当段月 `rgba(55,138,221,.10)` 底；空月刻度字墨 28% |
| 交互 | 月槽悬停 → 气泡两行「2025 年 M 月 / 值 + 『全园多发时它比全园还多｜跟得没那么足｜与全园同步』」；空槽第二行「样本不足｜还没到，这个月不出点」 |
| 取数 | `snap.slopes.get(id)`（`responseSlopes` 逐月 β）；`n < 3` 或 `se = Infinity` → 空槽写「样本不足」；投产前 = 早于 `BoardRow.firstDate` 的月。**画板示意值**：2–8 月七个值（0.96–1.11）、1 月样本不足 |

### §3.20 B12 · 逐刻度明细（抽屉）

| 项 | 内容 |
|---|---|
| 卡头 / hint | 「逐刻度明细」/「这一栋这一段，每天的原始读数」；表头逐字「日期 / 当日发电 度 / 比值 / 在不在范围内 / 备注」；表脚逐字「只列这一栋、只列本段 · 表内可上下滚，这里停在第 21–28 天，其余 20 天滚上去看 · 空行不是 0，是那天没抄表」 |
| 图种 | HTML 表 |
| 几何 | 5 列：日期 108 / 当日发电 度 104 / 比值 84 / 在不在范围内 96 / 备注 余宽；行高 32；可见 8 行、表内上下滚 |
| 颜色编码 | 出范围行左 2px 色条（红 / 琥珀）+ 该行数值与状态同色；缺抄行整行墨 3.5% 底、数值「—」、备注「没抄表」；数值 mono tabular |
| 交互 | 表内滚动；无气泡 |
| 取数 | `pv_reading.gen_total`（当日发电）+ `BoardRow.ratio / out / state`；备注「连续第 k 天」由 `runs` 算；未到的行不出现、漏抄的行出现。**画板示意值**：E座 8 月 21–28 日读数 1,280–1,799 度与比值 |

---

## §4 实施时要定的

| # | 没定的事 |
|---|---|
| 1 | 段控右侧说明句「含 p / q / σ 等统计量」：用户说 α 卡保持原样，但全站规矩屏上不写行话 —— 保留原句 / 改成大白话，二选一 |
| 2 | α 卡参照系「带 = 块自助 90% 区间」里的「块自助」：同上，保留 / 改大白话，二选一 |
| 3 | α 卡 hint 与参照系写「90% 区间」，`congenitalCheck` 实算 2.5% / 97.5%（95%）：改字 / 改算法，二选一 |
| 4 | B6 板数全空时的空态：画板示意态（淡点 + 说明栏 + 录入入口）/ v3 §6.5「整块不渲染，换一句 11px 事实 + 入口」，二选一 |
| 5 | 主卡芯片行放不下：13 枚实测总宽 1038px，1440 下卡内 1025（溢出 13px）、1366 下 951（溢出 87px）；画板没覆盖 —— 换两行（卡高 +34）/ 横向滚动 / 缩短栋名 / 未命中栋收进「其余 N 栋」 |
| 6 | 首屏 1366×768 超预算 86px（548 vs 462），判据脚与段控落在折线下：接受 / 大图画布 236 → 150 / KPI 行并进主卡 |
| 7 | 「读不出」的栋在芯片条上怎么显示（v3 队列有这一组；画板只有 KPI 计数，芯片没有这一态） |
| 8 | §4.3 的「未列出 ≠ 没问题」与措辞「未低于你设的线」画板上都没有：放回主卡哪里 / 不放 |
| 9 | 估带窗口 `baseNote`（§3.7「窗口必须写在屏上」）画板主卡没画：放判据脚 / 放图头事实句 |
| 10 | §6.2 新鲜度第二行「已抄 n/N · 覆盖 x%（按已过去算）· 算于」画板 KPI 行没有：塞进「数据到」瓦副行 / 另起一行 |
| 11 | KPI 两条迷你趋势线（出范围栋数、缺抄条数）没有现成序列：逐月跑一遍 `buildSnapshot` 算出来 / 不画迷你线 |
| 12 | A2 行点击要不要开抽屉（v3 的 B4 线段是抽屉入口；画板 A2 只有悬停） |
| 13 | 年档下 B3 画什么：v3 是 B4 替 B3，B4 已并入 A2 —— B3 改逐月 12 点同一图种 / 年档隐藏 B3 |
| 14 | L3 行序：画板按固定栋序，`lab.season` 已按年内极差降序（v3「排序是这张图的杠杆」）—— 用哪个 |
| 15 | L2 淡带阈值口径：现 `PvMeterAnaView` 的 L2 没有这条带，画板 0.13 是示意 —— 要定按什么算 |
| 16 | L7 表脚「导出的 CSV 里有」：光伏分栋屏现在没有导出 —— 做导出 / 删掉这半句 |
| 17 | B9 变点区间阴影（§3.3「必须给区间」，§6.6 B9 有）画板没画：补上 / 按画板不画 |
| 18 | 屏上文案 4 处写了结论，与 §05「不输出解释性结论」冲突 —— B9「水平抬上去了」、B10「是这次变化本身，不是新的异常」、L2「第二天多半还偏高」、L4「这个偏差不像是碰巧碰上的」：改成测量句 / 按画板 |
| 19 | 栋名列宽：A2 栋名右对齐于 82（padL 92），B8 / L7 是 96 —— A2 对齐到 96 / 按画板 |

---

## §5 废弃方案记录

防止有人又把它们做回来。对照板源：`../运维文档/设计稿/已实现/光伏分栋分析v4定稿-2026-09-13/QueueA.dc.html`、`QueueB.dc.html`、`Options.dc.html`、`Options2.dc.html`。

| 方案 | 结论 | 原因 |
|---|---|---|
| 上一版画布（`../运维文档/设计稿/被否/光伏分栋分析第一版-2026-09-13/Main.dc.html` 等，沿用 §6.7 墨阶体系） | **被否** | 用户：「新设计和旧版没有任何区别，视觉上还是一样，一样的排版，一样的颜色，一样的可视化选择」 |
| 主卡左栏楼栋列表：点开「其余 N 栋」撑高主卡 | **被否** | 用户：「逐月比值的楼栋列表又长又挤」 |
| 主卡左栏楼栋列表：钉高 + 栏内滚动 | **被否** | 用户：「更加无法接受…全部挤在一起的列表，要么重新推翻重做这个功能，要么找一个新的设计样式」（点开后撑高与栏内滚动两种都不接受） |
| 主卡 A · 网格总览（13 栋迷你图 5×3 + 下方大图） | **未选**（选 B 芯片条） | 大图矮一截；每格只看形状不看刻度 |
| 主卡 C · 逐日条带（13 栋 × 28 天格子 + 下方大图） | **未选** | 只看得到在不在范围内，看不到比值形状与幅度 |
| 主卡 D · 叠加大图（13 条线全画） | **未选** | 各栋比值水平差三倍，11 条灰线挤成一团；带只画得了一栋 |
| 主卡 E · 有事的栋各占一张图 | **未选** | 卡高随命中数变，命中多时页面很长 |
| α-2 · 偏离全园的双向条 | **未选**（α 保持横向排名条） | 条色不再区分期别 |
| α-3 · 棒棒糖 + 期别分组 | **未选** | 横轴换成年等效小时，画的不是 α |
| L3-2 · 分期三面板折线 | **未选**（保持 13×12 热力格） | 每面板 150px 宽塞 5–6 条线；三期面板整个是空的 |
| L3-3 · 全园月度带 + 选中栋线 | **未选** | 一次只看一栋，11 栋的个体差异压进一条带 |
| 「抛光结果可信吗」独立 s12 卡（三读数 + 收敛面积图） | **被否** | 用户：「抛光可信吗没看懂什么意思」；整卡删掉，并成 α 卡一句（L5） |
| T-2 · 三张状态瓦 | **未选** | 整卡已删 |
| T-3 · 收敛进度轨 | **未选** | 整卡已删；对数刻度被压成等距 |
| B4 Tufte 斜率图 + B5 棒棒糖（绝对水平档第一版） | **被否** | 协调方看截图实测：斜率图两端标签叠成团、中段线打结；棒棒糖锚点贴最左、11 根杆几乎等长；两张卡说一件事 |
| A1 · 哑铃图（去年 → 今年，对着 807.5 h） | **未选**（选 A2） | B座 这种只差 5 h 的两点几乎叠在一起 |
| A3 · 2024 vs 2025 散点 | **未选** | 11 栋只画得出 9 栋；点挤在右上角 |
| B7-a · 三段 100% 堆叠面积 | **未选**（B7 保持现状） | 看不到绝对度数；损耗那条还是只有 3–12px 厚 |
| B7-b · 上层度数面积 + 下层损耗率色条 | **未选**（B7 保持现状） | 色条只分四档，读不出具体几个点 |
| B8-b · 表 + 内嵌条 | **未选**（选 B8-a） | 没有图形对比，差距靠读数 |
| B8-c · 发电量 vs 合计收益散点 | **未选** | 看不出自用 / 上网各多少；中间六栋挤成一团 |
