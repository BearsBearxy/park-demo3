-- c402-rebind.sql — C座402室三块表(314/315/464)改绑 可莱恩(107)→中科美业(108)。2026-08-05。
-- ⚠ 仅起草,未执行。执行前先备份 meter 表(参照 backup-before-meter-owner-fix-20260730.sql 做法)。
-- 幂等:按 id 精确 UPDATE + 前值护栏,可重复执行(第二次命中 0 行)。
--
-- ══════════════════════════════════════════════════════════════════════════════
-- 来龙去脉
-- ══════════════════════════════════════════════════════════════════════════════
-- 对账(scripts/verify_bill_notice_202402_report.txt):中科美业整户缺 电/水/管理费/损耗 ~20行,
-- 可莱恩多出同额 ~15行。根因=三块表档案 tenant_id=107(可莱恩),而 Excel 真值计给中科美业。
--
-- Excel 证据(一期2024年2月水电费.xlsx):
--   · 「中科美业」sheet(第25个):
--       B3/J3  租户名称:中科美业(广东)药业科技有限公司,位置:C座402室
--       A6~A15 电表1峰/尖/平/谷 + 电表2峰/尖/平/谷,A列别名即「可莱恩C402电1/电2」
--              (电1 E6=2504.37→F6=2521.81 倍率60;电2 E11=1668.82→F11=1690.98 倍率20)
--       A25    「可莱恩C402水」E25=6310→F25=6507,K25=778.15
--       K16=12094.62(电费) K22=1179(维护) K28=896.23(水) K32=14169.85(总计)
--       O42 汇总标题「2024年2月水、电数据汇总——中科美业」,P44 归「一泽公司/C座」列
--   · 「可莱恩」sheet(第24个):
--       J3 位置串虽仍写「A座602室、B座201室、C座402室、宿舍」(旧模板残留),
--       但正文只有 A602/B201/宿舍 三段,无任何 C402 计费段;
--       汇总 R87「C座」列 R99=0 / R105=0 / R107=0 ⇒ C402 在可莱恩名下计 0。
--   ⇒ 不是转租期拆段(两家都算),而是整段计给中科美业,一刀切改绑安全。
--
-- 库内证据(dev park_demo3):
--   · meter 314(可莱恩C402电1,code 202001000004)/315(电2,code 202001000036):
--       tenant_id=107,contract_id=406 —— 406 是可莱恩 B座201(unit 429)合同,与 C402 无关,纯错挂。
--   · meter 464(可莱恩C402水):tenant_id=107,contract_id=NULL,active_from_ym=2024-02。
--   · 三块表 owner_manual=0 / loc_manual=0,无人工档案覆盖需保护。
--   · unit 444 = 一期C座(building 21) 4楼402;唯一挂它的合同 = contract 6(S10-0006,
--     tenant 108 中科美业,2023-11-01→2029-08-06,active)。2024-02 落在合同期内,
--     且可莱恩名下(合同 405/406/57/95/5 + contract_unit)从无 C402 ⇒ 非时点性转租,
--     现在与当时都该归中科美业,直接改档案。
--   · 三块表各只有 1 条读数(均为 2024-02),无跨期波及。
--
-- 不改的:meter.name 保留「可莱恩C402电1/电2/水」—— 名称=导入身份键(三层身份重构),
-- 改名会导致重导 Excel 时匹配不上、静默新建重复表。名称只是历史称呼,归属以 tenant_id 为准。
-- ══════════════════════════════════════════════════════════════════════════════

-- 执行前核对(应各返回 1 行,tenant_id=107):
-- SELECT id,name,tenant_id,tenant_name,contract_id FROM meter WHERE id IN (314,315,464);

-- 电表两块:清 contract_id(原406为错挂),留空让五级归属规则按 unit 444→contract 6 重解
UPDATE meter
   SET tenant_id = 108,
       tenant_name = '中科美业',
       contract_id = NULL
 WHERE id IN (314, 315)
   AND tenant_id = 107
   AND contract_id = 406;

-- 水表:contract_id 本就 NULL,只改归属
UPDATE meter
   SET tenant_id = 108,
       tenant_name = '中科美业'
 WHERE id = 464
   AND tenant_id = 107
   AND contract_id IS NULL;

-- 执行后核对(应 3 行均 tenant_id=108/中科美业,contract_id 均 NULL):
-- SELECT id,name,tenant_id,tenant_name,contract_id FROM meter WHERE id IN (314,315,464);
-- 然后重生成 2024-02 催缴单,预期:中科美业补回 C402 电/水/管理费/损耗 ~20行(总计锚 14169.85),
-- 可莱恩减去同源 ~15行,该户对账差额清零。
