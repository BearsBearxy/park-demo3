# S6 场地标签按表定位 · 2024-02 重生成对账

规范：`docs/design/S6-PREMISE-BY-METER-SPEC.md` §6
备份：`backend/scripts/fixes/backup-before-s6-20260808.sql`（bill_notice + bill_notice_line）
基线快照：dev 库临时表 `s6_base_notice` / `s6_base_line`（批次 `BN20260807225509`，2026-08-07 22:55:09）
新批次：`BN20260808040727`（2026-08-08 04:07:27），`generated=290 / lines=3363 / warned=200`

---

## 一、基线校正（必读，否则会误判为回归）

重生成后行数 3359 → 3363（+4）、合计 3,535,971.77 → 3,558,989.77（+23,018.00）。
**这 4 行与 23,018.00 全部不是 S6 造成的**，是基线生成之后的数据漂移：

合同 131（S10-0131 碧沃丰）的 6 条计费行 `created_at = 2026-08-07 **22:59:04**`，比基线批次晚 4 分钟——
是上一轮修好合同编辑器（其他费用独立标的 / 空地 infra 面积×单价）之后保存该合同新建的。

新增 4 个费项与金额逐项对得上：

| 费项 | 算式 | 金额 |
|---|---|---:|
| mgmt | 3200 × 5.00 | 16,000.00 |
| infra | 3200 × 2.00 | 6,400.00 |
| elevator | per_month override | 318.00 |
| transformer | per_month override | 300.00 |
| **合计** | | **23,018.00** |

`3,535,971.77 + 23,018.00 = 3,558,989.77` ✅ 一分不差；`3359 + 4 = 3363` ✅ 行数也对得上。

→ **校正后基线 = 290 单 / 3363 行 / 3,558,989.77**，S6 相对它必须逐格全等。

---

## 二、§6.1 金额护栏

| 锚点 | 校正后基线 | 重生成实测 | 判定 |
|---|---|---|---|
| 全月单数 | 290 | **290** | ✅ |
| 全月行数 | 3363 | **3363** | ✅ |
| 全月合计 | 3,558,989.77 | **3,558,989.77** | ✅ |
| 损耗行数 `share_elec_loss` | 124 | **124** | ✅ |
| 损耗行合计 | 12,643.23 | **12,643.23** | ✅ |
| 可莱恩 combined | 65,062.55 / 31 行 | **65,062.55 / 31 行** | ✅ |
| 可莱恩 dorm | 6,135.18 / 54 行 | **6,135.18 / 54 行** | ✅ |
| 可莱恩 combined(2) | 50,477.60 / 4 行 | **50,477.60 / 4 行** | ✅ |

**全库逐格比对**：按（租户 × 单类 × 费项）聚合 count + Σamount 做全外连接，
**唯一差异就是碧沃丰那 4 条漂移行**（old NULL → new 1 条），其余每一组行数与金额全等。

> 损耗那两格是规范 §2.6 的直接指标。若不做 F1 的粗粒度分桶修复，实测会变成 **146 行 / 12,643.21**（21 户受影响，
> 旭化成一条 758.68 裂成 482.55 + 276.13）。`mvnw verify` 全绿抓不到这条——IT 对损耗链零覆盖。

---

## 三、§6.2 标签效果

| 指标 | 改前 | 规范目标 | 实测 | 判定 |
|---|---:|---|---:|---|
| 宿舍表行 premise 含「、」 | 409 | 39（D3 27 + D4 12） | **39** | ✅ |
| 非宿舍表行 premise 含「、」 | 345 | 约 165 | **165** | ✅ |
| premise 恰好 64 字（**表行**口径） | — | 31，全部 cid 359 | **31**（cid 359：elec 13 / water 8 / mgmt_fee 7 / water_pipe 3） | ✅ |
| premise 恰好 64 字（**全部行**口径） | 276 | — | **48**（表行 31 + 非表行 17） | 见注 |
| 场地未定告警 | — | 只剩 zero_hit | **72 张单** | ✅ |

> **注**：另外 17 条非表行（cid 359 的 share_*/capacity、cid 52 的 share_elec_loss/capacity、cid 439 的
> rent_dorm/infra/access/network/share_*）走 `premiseOf` 或计费行 location 原文，S6 按 §4 刻意不改，
> 它们改前也是 64 字。规范 §6.2 写的 31 是表行口径，两者各自成立。

### 宿舍残留 39 行逐条归因（无一归不进去）

| 合同 | 行数 | premise 样例 | 归属 |
|---|---:|---|---|
| 359 | 15 | `二期13座二楼、二期13座天面、宿舍楼四座229室、宿舍…` | D3 表挂错合同 |
| 449 | 12 | `保障房2号楼、保障房3号楼` | **D4** 开利暖通整栋表误标 `is_dorm_room=1` |
| 58 | 3 | `宿舍一座307室、宿舍一座314室` | D3 |
| 7 | 3 | `D座403室、宿舍四座442室` | D3 |
| 299 | 3 | `A座孵化器四楼439室、440室` | D3 |
| 298 | 2 | `A座孵化器A102室、宿舍545室` | D3 |
| 66 | 1 | `宿舍区3号楼首层3101室、宿舍区3号楼首层4101室、` | D3 |

D3 = 27 行 / 6 份合同，D4 = 12 行 / 1 份合同，合计 39 ✅

### 目标形态实证（广联合同 439，改前是 124 字被截成 64 字的 30 房号串）

| 表 | name | premise |
|---|---|---|
| 594 | 309 | `宿舍一栋309室` |
| 595 | 310 | `宿舍一栋310室` |
| 596 | 311 | `宿舍一栋311室` |
| 598 | 313 | `宿舍一栋313室` |
| 600 | 315 | `宿舍一栋315室` |
| 601 | 316 | `宿舍一栋316室` |

---

## 四、告警面变化（需用户拍板一项）

`warned` 179 → 200（+21 张单），`场地未定` 覆盖 72 张单。构成（离线复算口径，按表）：

- 收窄掉 **16 块**误报：`no_token` 14（开利暖通整栋表、翔海借电 D/E/F、三车间工地…）+ `multi_hit` 2（桑尼号电、炳记电）
- 新增 **38 块**：原先因回退串不含「、」而静默的**单-location 合同上的真零命中**
  （星州 4 / 永龙 4 / 金纳 D301 3 / 飞浪 3 / 彭云霞 2 …，典型形态=宿舍表挂在只写了车间房号的合同上）

按 §2.4 定义这 38 块都是真 D3，语义无误；但净结果是告警**变多**而非变少。
**待拍板**：单-location 合同上的零命中要不要报。不报则少 38 条噪音，代价是这批 D3 数据债从此不可见。

warn 串截断风险已排除：最长一户（邓宇峰 15 块表）164 + 10 = 175 / 255，全户无一超限，`缺起止日期` 未被挤出。

---

## 五、其它行为变更（不影响金额，需知悉）

| 变更 | 量 | 说明 |
|---|---:|---|
| `line_no` 重排 | 61 / 290 单 | premise 分组序变了 → 单内行序变。尤其损耗行 premise 仍是粗粒度串，不再与本组表行同键，会从表行中间移到独立分组。**人工对照过的旧截图/导出件会对不上** |
| `premise_text` 变化 | 64 / 290 单 | 其中 11 单触发「超 5 项 + 等N处」收敛；3 单是 premise 未细化、纯被收敛规则改写（原文本未超 255） |
| 非宿舍单公摊块分家 | 30 / 227 单 | 规范 §4 已记的已知代价，S6-2 修复前不要报成 bug |
| 宿舍逐间路灯/绿化水配对 | 12 → 142 挂上，extras 176 → 46 | 前端 §3 配对键改按表唯一后的收益（离线按真规则模拟）。**已在库中实证**：合同 442 电表行 premise `宿舍楼四座339室` 与 `share_elec_light` 行逐间全等，全月 72 组 (notice, premise) 对齐 |
| 列表「位置」列出现多个「等N处」 | 多单户可见 | `aggregateByTenant` 把各单 `premise_text` 按逗号拆项去重，后端的「等N处」标记也被当成一个场地项。广联（4 张单）显示为 `…,等7处,…,等52处,…,等22处`。纯显示毛刺、有 tooltip 兜底，未改；若要修应在前端按租户级重新收敛一次 |

---

## 六、遗留

- 临时表 `s6_base_notice` / `s6_base_line` 仍在 dev 库，确认无误后可 `DROP`。
- 数据债 D1/D2（合并 location 拆计费行，需逐间面积）与 D3（40+ 行表挂错合同）待用户拍板，见规范 §5。
