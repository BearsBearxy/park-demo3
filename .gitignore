# Java / Maven
target/
*.class
backend/logs/
# 本地 dev 后端 stdout/stderr 落盘
backend/dev-run.*

# 工具生成物(frontend/src/tools/buildingRestructure.ts 输出)
restructure-plan.csv

# Node / Vite
node_modules/
frontend/dist/
*.local

# Env / secrets
.env
*.env
!.env.example

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# MySQL data volume (compose)
mysql-data/

# SDD progress ledger (git-ignored scratch)
.superpowers/

# 本地数据库备份(勿入库勿上传)
backup-*.sql

# 本地 dev 前端 stdout/stderr 落盘
frontend/dev-fe.*
# 一次性数据修复的库表备份(与 backup-*.sql 同类,勿入库)
backend/scripts/backups/

# Google Fonts 原始下载包(600+KB 的 TTF 全字重)。入库的只有子集后的
# frontend/public/fonts/*.woff2(47KB)+ OFL 许可;要重新子集时再下一次即可,
# 命令见 frontend/src/styles/tokens.css 的 @font-face 注释。
Roboto_Mono/
Roboto_Mono.zip

# 设计画布的产物(2.5MB 编辑器载荷,非内容);源文件 _design/*.dc.html 照常入库
_design/*.html
!_design/*.dc.html
